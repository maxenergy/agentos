# AgentOS Completion Review And Plan

Last updated: 2026-05-06

This file is the working plan for aligning the implementation with the docs. Update it whenever a task is completed, deferred, or re-scoped.

## Update Rules

- Mark completed tasks with `[x]` in the same commit/change set that implements them.
- Add a short note under "Progress Log" for every completed milestone.
- Keep `docs/ARCH_ALIGNMENT.md` as the architecture truth source, and update older design docs when they drift.
- Do not add new capabilities before policy, audit, tests, and persistence are covered for that capability.

## Completion Snapshot

Current review: AgentOS is a runnable local MVP with resident operation modes (Interactive REPL and HTTP API server), not a production-complete system. Rough completion is about 92% for MVP scope and 60-65% for product-grade scope. The latest local verification passes all 17 CTest targets in `build-codex-g014`.

| Area | Current Status | Evidence | Remaining Gap |
| --- | --- | --- | --- |
| Core Runtime | Implemented MVP | `AgentLoop`, `Router`, `SkillRouter`, `AgentRouter`, `WorkflowRouter`, `PolicyEngine`, `AuditLogger`, registries, Interactive REPL, HTTP API Server | Failure recovery, lifecycle/session semantics |
| Builtin Skills | Implemented MVP | `file_read`, `file_write`, `file_patch`, `http_fetch`, `workflow_run`, required input schema validation, scalar `properties.*.type`, `const`, `enum`, length, pattern, `additionalProperties:false`, `propertyNames`, `minProperties` / `maxProperties`, inclusive/exclusive numeric range, `multipleOf`, `dependentRequired`, legacy `dependencies`, object-level `not.required`, basic `if` / `then` / `else`, and `allOf` / `anyOf` / `oneOf` required-branch validation | Richer JSON Schema keywords and richer workflow definitions |
| CLI Integration | Implemented MVP | `CliHost`, CLI skill invoker, repo-local external spec loader with source file/line metadata, empty-field-safe TSV parsing, duplicate-name diagnostics, registered-skill conflict audit/validate checks, parse-mode/risk/permission diagnostics, required-arg/template consistency diagnostics, schema-object diagnostics, strict numeric-field diagnostics with positive timeout and non-negative resource limits, startup audit events for skipped specs, `agentos cli-specs validate`, `jq_transform`, cwd/timeout/output/env controls, sensitive argument redaction, Windows Job Object and POSIX `setrlimit` resource controls for process/memory/CPU limits, plus POSIX file descriptor limits | Windows file-handle limits and richer sandboxing |
| Agent System | Partial | `IAgentAdapter`, `local_planner` local deterministic planning adapter with session tracking and structured plan output plus role-annotated plan steps, `codex_cli`, `gemini` generateContent provider adapter, `anthropic` Messages/Claude CLI adapter, `openai` Chat Completions adapter (sync + V2 SSE streaming, default gpt-4o), `qwen` OpenAI-compatible Chat Completions adapter, per-task `profile=` auth profile override for `run` / `schedule add` / `subagents run`, normalized `agent_result.v1` structured output, `SubagentManager`, deterministic role assignment, explicit and planner-generated per-agent subtask objective inputs, role/agent-targeted `plan_steps[]` decomposition, step-level structured output retention and normalized `agent_outputs` aggregation, automatic subagent candidate selection, `WorkspaceSession` abstraction, subagent cost/concurrency limits | More advanced model-driven complex task decomposition |
| Auth System | Partial | Auth manager, provider adapters, API-key env refs, Gemini CLI browser-OAuth passthrough import, CLI session probes/import tests, refresh command/adapter path, workspace default profile mapping, `auth profiles` session/profile discovery, `set_default=true` one-step default profile selection for login/OAuth completion, Windows Credential Manager backend for managed tokens, macOS Keychain backend, optional Linux Secret Service backend with env-ref dev fallback, reusable OAuth PKCE start/callback URL validation scaffolding, Gemini Google OAuth default authorization/token endpoints and scopes, OpenAI PKCE default authorization/token endpoints (`auth.openai.com`) and scopes, repo-local `runtime/auth_oauth_providers.tsv` OAuth defaults overrides, `auth oauth-config-validate` diagnostics, `auth oauth-defaults` provider OAuth discovery, `auth oauth-start` authorization URL generation with optional `open_browser=true` system-browser launch, `auth oauth-login` start/listen/token-exchange/session-persist orchestration, `auth oauth-callback` callback URL parsing/state validation, `auth oauth-listen` one-shot loopback callback capture, authorization-code and refresh-token request form-body construction, curl-backed exchange helpers, token response parsing, managed AuthSession persistence helpers, `auth oauth-complete`, parameterized provider adapter native OAuth login/refresh completion, `auth oauth-token-request`, `auth oauth-refresh-request`, Auth Profile default selection, and fixture-level Auth Login Flow mode tests | Anthropic public PKCE defaults remain deferred until stable customer endpoints exist; cloud credential modes beyond current Google ADC remain future work |
| Memory And Evolution | Partial | Task/step logs, skill/agent stats, LessonStore, lesson-driven routing/policy hints, workflow candidates/scoring, durable WorkflowStore, promotion command with validation, stored workflow execution, Router workflow preference, required-input, exact-match, numeric-range, boolean-input, regex-input, `input_any` composite OR, `input_expr` nested boolean applicability, filtered `memory stored-workflows`, `memory show-workflow`, `memory clone-workflow`, `memory update-workflow`, `memory set-workflow-enabled`, `memory remove-workflow`, `memory validate-workflows`, and `memory explain-workflow` | Richer workflow definition editing UX |
| Identity / Trust | Implemented MVP | Identity store, one-time pairing invite handshake with TTL, paired-device lifecycle metadata and show/manage commands, pairing, allowlist, TrustPolicy, per-task permission grants, persistent RoleCatalog user-role grants | Richer device fleet admin UX |
| Scheduler | Implemented MVP | persisted one-shot/interval/cron tasks, `run-due`, foreground `tick` loop, foreground `daemon` loop, run history metadata, retry/backoff, `every:<n>s|m|h|d` recurrence, five-field cron expressions plus `@hourly` / `@daily` / `@weekly` / `@monthly` / `@yearly` / `@annually` / `@midnight` / `@noon` aliases, DOM/DOW OR semantics, `missed_run_policy`, disabled/missed-run coverage, modular `cron` + `timezone` evaluator with optional `timezone=` (UTC default, fixed offsets, curated IANA zones with DST disambiguation), tz-aware persistence | Wider IANA zone coverage if needed |
| Policy / Permissions | Implemented MVP | PermissionModel, risk parsing, unknown permission deny, per-task permission grants, persistent RoleCatalog user-role grants plus remove/show commands, persistent ApprovalStore with request/show/approve/revoke and approved approval_id enforcement for CLI runtime, side-effect idempotency enforcement, CLI/audit sensitive value redaction | Richer admin UX |
| Plugin Host | Partial | `PluginHost`, `PluginSkillInvoker`, `runtime/plugin_specs/*.tsv`, `runtime/plugin_specs/*.json`, `plugin.v1` TSV/JSON manifest, source file/line metadata for loaded/skipped/conflicting specs, empty-field-safe TSV parsing, duplicate-name diagnostics, registered-skill conflict audit/validate checks, risk/permission diagnostics including required `process.spawn`, required-arg/template consistency diagnostics, health-probe placeholder diagnostics, schema-object diagnostics, strict numeric/bool-field diagnostics with positive timeout and non-negative resource limits, spec health validation/reporting, optional `health_args_template` probes, skipped-spec diagnostics with startup audit events, `agentos plugins`, scriptable `agentos plugins validate`, scriptable `agentos plugins health`, scriptable `agentos plugins lifecycle`, scriptable `agentos plugins inspect name=<plugin> [health=true]` manifest inspection and optional single-plugin health probe, `stdio-json-v0` JSON-object stdout and `json-rpc-v0` JSON-RPC 2.0 result-object process protocols, persistent `json-rpc-v0` Plugin Sessions with request IDs, stdin/stdout round-trips, timeout handling, stderr capture, failure-session eviction/restart, destructor shutdown, lifecycle-aware health round-trips, active-session counting, manual close/restart/prune, idle-timeout restart, workspace-configurable `runtime/plugin_host.tsv` max persistent session cap with per-plugin/workspace/binary LRU eviction, process-scoped admin diagnostics with `scope=process persistence=none`, unsupported future scope diagnostics, `output_schema_json.required` / `properties.*.type` / string `const` / `enum` / length / pattern / array `minItems` / `maxItems` / `uniqueItems` / `prefixItems` / `contains` / `minContains` / `maxContains` / `items.type` / `items.const` / `items.enum` / `items.minLength` / `items.maxLength` / `items.pattern` / `items.minimum` / `items.maximum` / `items.multipleOf` / object-item `items.required` / `items.properties.*.type` / `items.propertyNames` / `items.dependentRequired` / `items.dependencies` / `items.not.required` / `items.additionalProperties:false` / `items.minProperties` / `items.maxProperties` / `additionalProperties:false` / `propertyNames` / `minProperties` / `maxProperties` / `dependentRequired` / `dependencies` / `not.required` / `if` / `then` / `else` / numeric range / `multipleOf` / `allOf` / `anyOf` / `oneOf` required-branch validation, structured Capability Output envelope, CLI resource limit pass-through, and `sandbox_mode=workspace|none` with workspace path-argument containment | Future daemon-scope Plugin Session administration if a durable cross-process need emerges |
| Storage | Prototype | TSV files under `runtime/`, atomic replacement for full-file TSV stores, locked append-only logs with append-intent recovery, same-process concurrent writer coverage, POSIX cross-process TSV backend append coverage, transactional multi-file write helper with prepare/commit/recover markers, `storage recover` with corrupt committed transaction isolation and `failed=` reporting, `runtime/storage_manifest.tsv` version metadata, `storage status` file existence/bytes/line-count inspection, `storage verify [src=<directory>] [strict=true]` manifest completeness diagnostics, manifest-based export/import with pre-import backups under `runtime/.import_backups/`, `storage backups` discoverability and `storage restore-backup` rollback for import backup directories, explicit `storage migrate` helper with legacy path migration into `runtime/` plus schema normalization for memory/workflow/scheduler stores, TSV-backed `StorageBackend` seam, storage compaction for task/step/scheduler logs, audit compaction plus intentionally lossy task lifecycle rebuild from `runtime/memory` and scheduler-run rebuild from `runtime/scheduler/runs.tsv`, explicit TSV MVP policy | SQLite or broader cross-format migration; full-fidelity audit reconstruction remains intentionally out of scope for current recovery fallback |
| Tests | Module-level coverage + CLI integration + basic CI | 17 local CTest targets including CLI integration, storage, auth, agent providers, Agent Dispatch, policy/trust, scheduler, workflow router, subagent sessions, cancellation, plugin, spec parsing, file-skill policy, host info, learned skill, chat fallback, and main-agent prompt tests; GitHub Actions configure/build/test on Windows + Ubuntu; added negative-path coverage across policy/trust/scheduler/auth plus shared cross-platform command fixtures | Expand failure/regression suites |
| Docs | Mostly current | `ARCH_ALIGNMENT.md`, `ROADMAP.md`, and this plan are synced | Some design docs still need ongoing gap annotations as implementation changes |

## Review Findings

- Current workspace state passes `cmake --build build-codex-g014 && ctest --test-dir build-codex-g014 --output-on-failure` with 17/17 tests passing.
- The system is broad enough for local MVP validation, but production readiness still depends on auth hardening, plugin long-running lifecycle, stronger storage recovery, and richer agent orchestration.
- `docs/ROADMAP.md` has now been synced to the current implementation state, but it must stay linked to this plan to avoid drifting again.
- `AUTH_PRD.md` and `AUTH_DESIGN.md` describe OAuth, refresh, cloud credentials, and secure credential storage; current code implements API-key env references, Windows Credential Manager managed-token storage, Codex/Claude CLI session probing, Gemini CLI browser-OAuth passthrough, native PKCE authorization-code / refresh-token exchange helpers, managed session persistence, Gemini OAuth defaults, repo-local OAuth defaults overrides, `oauth-config-validate`, `auth profiles`, `set_default=true`, and `oauth-login` single-command OAuth orchestration. Remaining Auth gaps are broader provider-specific OAuth discovery, fuller multi-provider product UX, non-Windows credential stores, cloud credential modes, and a complete multi-account strategy.
- Workflow learning now has candidate/scoring output, LessonStore, lesson-driven routing/policy hints, durable WorkflowStore, manual promotion with validation, stored workflow execution, Router preference, required-input / exact-match / numeric-range / boolean-input / regex-input / `input_any` composite OR / `input_expr` nested boolean applicability checks, filtered `memory stored-workflows`, `memory show-workflow` inspection, `memory clone-workflow`, `memory update-workflow`, `memory set-workflow-enabled`, `memory remove-workflow`, `memory validate-workflows` diagnostics, and `memory explain-workflow` applicability explanations.
- Scheduler supports manual `run-due`, foreground `tick`, foreground `daemon`, run history metadata, retry/backoff, `missed_run_policy=run-once|skip`, `every:<n>s|m|h|d` recurrence, and five-field cron expressions with `*`, `*/n`, single values, comma lists, ranges, `@hourly` / `@daily` / `@weekly` / `@monthly` / `@yearly` / `@annually` aliases, and DOM/DOW OR semantics. Disabled tasks are skipped, and missed interval/cron tasks can either run once per tick or skip stale runs and reschedule from the current scheduler time.
- Multi-agent orchestration supports explicit lists, deterministic role assignment, explicit per-agent subtask objectives, `auto_decompose=true` planner-generated subtask objectives with optional `plan_steps[].role` / `plan_steps[].agent` targeting, default `local_planner` role annotation from decomposition context, step-level structured output retention, top-level normalized `agent_outputs` aggregation, automatic healthy/capability-based subagent candidate selection, `WorkspaceSession` for session-capable agents, parallel concurrency limits, and estimated-cost budget checks. More advanced model-driven complex task decomposition remains open.
- Agent execution now accepts per-task `profile=` / `auth_profile=` overrides and forwards them through `AgentLoop`, `SubagentManager`, scheduled tasks, and provider adapters so non-default auth profiles can be selected without changing workspace defaults. End-to-end coverage lives in `tests/cli_integration_tests.cpp::TestRunAuthProfileOverride`, which drove the discovery and fix of four V2→legacy projection helpers (`Qwen/OpenAI::InvocationToTask`, `Gemini/Anthropic::TaskFromInvocation`) that previously dropped `auth_profile` (and, for qwen/openai, `context_json`) when the V2 invoke path fell back to sync `run_task`. The resident operation modes have been brought to parity: Interactive REPL `run` accepts `profile=` / `auth_profile=` (previously dropped into `task.inputs`) and the HTTP API `/api/run` accepts top-level `profile` / `auth_profile` JSON fields, both flowing through the same `TaskRequest.auth_profile` plumbing.
- Plugin Host now has repo-local `plugin.v1` manifests with `stdio-json-v0` JSON-object stdout and `json-rpc-v0` JSON-RPC 2.0 result-object process protocols through `runtime/plugin_specs/*.tsv` and `runtime/plugin_specs/*.json`, source file/line metadata for loaded/skipped/conflicting specs, empty-field-safe TSV parsing, duplicate-name diagnostics, registered-skill conflict audit/validate checks, risk/permission diagnostics including required `process.spawn`, required-arg/template consistency diagnostics, health-probe placeholder diagnostics, schema-object diagnostics, strict numeric/bool-field diagnostics, positive timeout and non-negative resource limit checks, structured health reporting with optional `health_args_template` probes, skipped-spec diagnostics, startup audit events for skipped specs, `agentos plugins`, scriptable `agentos plugins validate`, scriptable `agentos plugins health`, scriptable `agentos plugins lifecycle`, scriptable `agentos plugins inspect name=<plugin> [health=true]`, `output_schema_json.required` / `properties.*.type` / string `const` / `enum` / length / pattern / array `minItems` / `maxItems` / `uniqueItems` / `prefixItems` / `contains` / `minContains` / `maxContains` / `items.type` / `items.const` / `items.enum` / `items.minLength` / `items.maxLength` / `items.pattern` / `items.minimum` / `items.maximum` / `items.multipleOf` / object-item `items.required` / `items.properties.*.type` / `items.propertyNames` / `items.dependentRequired` / `items.dependencies` / `items.not.required` / `items.additionalProperties:false` / `items.minProperties` / `items.maxProperties` / `additionalProperties:false` / `propertyNames` / `minProperties` / `maxProperties` / `dependentRequired` / `dependencies` / `not.required` / `if` / `then` / `else` / numeric range / `multipleOf` / `allOf` / `anyOf` / `oneOf` required-branch validation, structured `plugin_output` embedding for successful plugin output, pass-through for CLI resource limit fields, `sandbox_mode=workspace|none` with workspace path-argument containment, reusable long-running JSON-RPC process sessions, and configurable max persistent session cap with LRU eviction; deeper process-pool policy and richer runtime session admin UX remain open. External CLI spec loading supports repo-local `runtime/cli_specs/*.tsv` files with source file/line metadata, empty-field-safe TSV parsing, duplicate-name diagnostics, registered-skill conflict audit/validate checks, parse-mode/risk/permission diagnostics, required-arg/template consistency diagnostics, schema-object diagnostics, strict numeric-field diagnostics, positive timeout and non-negative resource limit checks, startup audit events for skipped specs, and `agentos cli-specs validate` diagnostics.
- Skill execution now validates required input fields, basic scalar property types, const values, enum membership, string length, regex pattern, unexpected inputs when `additionalProperties:false`, input field-name constraints via `propertyNames`, object property count via `minProperties` / `maxProperties`, inclusive/exclusive numeric range, `multipleOf`, `dependentRequired`, legacy `dependencies`, object-level `not.required`, basic `if` / `then` / `else`, and `allOf` / `anyOf` / `oneOf` required-branch constraints declared in `input_schema_json` before invoking the selected skill.
- Full-file TSV stores now write through a temporary file and atomic replacement to reduce crash-corruption risk.
- Append-only logs now use the same single-writer lock helper before appending audit/task/step/scheduler history records, and write append-intent files so the next append can recover an interrupted write or remove a stale already-applied intent.
- Runtime now maintains `runtime/storage_manifest.tsv` so storage format/version metadata is explicit and inspectable.
- `agentos storage migrate` now makes manifest/version normalization an explicit maintenance action instead of only an implicit startup side effect.
- `agentos storage migrate` now also migrates legacy root-level storage files into the current `runtime/` layout, normalizes known legacy memory/workflow/scheduler schemas to the current TSV shape, and reports `migrated_files`.
- `agentos storage export dest=...` can export manifest-managed runtime state to a separate directory.
- `agentos storage import src=...` can restore manifest-managed runtime state from an exported directory and first backs up existing managed files under `runtime/.import_backups/`.
- `agentos storage backups` lists available pre-import backup directories with relative paths, file counts, byte counts, and a summary for scripting rollback workflows.
- `agentos storage restore-backup name=<backup_name>` restores a listed pre-import backup by directory name and backs up the current overwritten files before rollback.
- `agentos storage compact` now rewrites task/step/scheduler logs from parsed state and can rebuild task-related audit lifecycle entries from `runtime/memory` plus `scheduler_run` audit events from `runtime/scheduler/runs.tsv` even if `audit.log` is missing, while preserving non-task audit events, reinserting task-scoped preserved events like `policy` alongside the recovered task flow, replacing stale scheduler-run audit rows with scheduler history state, preserving orphan task-scoped preserved events for tasks no longer present in memory logs, reusing prior lifecycle timestamps when available, merging task bundles, scheduler-run chunks, and timestamped preserved task events into a stable chronological order, and placing untimed global preserved events after timed chunks in original order.
- `agentos storage status` now reports the current storage policy and per-component file existence, byte size, and line count; `agentos storage verify [src=<directory>] [strict=true]` provides scriptable manifest completeness diagnostics before export/import workflows; TSV remains the MVP backend and SQLite migration is deferred until stronger transactional/query needs appear.
- Persistence is TSV-based and adequate for MVP, but not yet transactional and still lacks broader cross-format migration and full-fidelity audit reconstruction.
- Identity / Trust now includes a persisted pairing-invite handshake: `trust invite-create` creates one-time TTL-bound invites, `trust invite-accept` consumes them into the allowlist, and `trust invites` lists active invitations.
- Paired devices now carry lifecycle metadata (`paired_epoch_ms` and `last_seen_epoch_ms`) and can be renamed, marked seen, blocked, unblocked, or removed through `agentos trust`.
- Policy approvals now have persistent records: `trust approval-request` creates a pending approval, `trust approval-approve` authorizes it, `trust approval-revoke` revokes it, and CLI runtime high-risk policy requires `approval_id` to reference an approved record.
- Trust/Policy admin UX now includes `trust role-show`, `trust user-role-show`, and `trust approval-show` for scriptable single-record inspection with explicit not-found exits.
- Trust device admin UX now includes `trust device-show` for scriptable paired-device inspection with lifecycle metadata and explicit not-found exits.

## Next Execution Plan

### Current Highest-Priority Gaps

- Auth hardening: Anthropic public PKCE defaults remain blocked on stable customer endpoints; cloud credential modes beyond current Google ADC remain future work.
- Plugin lifecycle: persistent JSON-RPC Plugin Sessions are implemented with process-scoped admin commands, configurable caps, per-plugin/workspace/binary LRU eviction, `plugin_host.tsv` diagnostics, and scriptable unsupported-scope diagnostics; future daemon-scope admin remains out of scope until a durable need emerges.
- Storage reliability: the TSV-backed `StorageBackend` seam and cross-process append coverage are in place; remaining work is deciding when to move beyond TSV MVP and preserving the intentionally lossy Audit History recovery boundary.
- Agent orchestration: normalized cross-agent result schema is available; remaining work is richer model-driven complex task decomposition.
- Scheduler completeness: timezone/DST-specific calendar hardening if product requirements need it.

## Completion Review Validation: 2026-04-25

`completion_review.md` is mostly directionally correct for MVP/product readiness, but it is not exact against the current workspace:

- Correct with updates: `oauth-defaults` exposes built-in provider OAuth discovery, `oauth-start` can optionally launch the system browser, and `oauth-login` now orchestrates start/listen/token-exchange/session persistence in one command. Plugin Host has reusable long-running JSON-RPC lifecycle support, process-scoped admin commands, and focused modules. TSV remains the MVP storage backend behind a `StorageBackend` seam. Gemini and OpenAI have built-in OAuth defaults; Anthropic PKCE remains deferred while stable public endpoints are unavailable.
- Outdated: Plugin sandboxing is no longer missing. `PluginSpec.sandbox_mode` now supports `workspace|none`, and default workspace mode rejects path-like runtime arguments that resolve outside the active workspace.
- Overstated: Phase H should not be considered fully production-complete while broader cross-format migration remains open.
- Current verification: full local `cmake --build build-codex-g014 && ctest --test-dir build-codex-g014 --output-on-failure` passed 17/17 tests after the goals and follow-up testing rounds.

## Post-Review Development Plan

### Phase J: Auth Production Hardening

- [x] Add a credential-store abstraction that can report and select `env-ref-only` or Windows Credential Manager backends.
- [x] Implement a Windows Credential Manager backend first, with read/write/delete/status tests guarded for Windows.
- [x] Add macOS Keychain backend via the Security framework (generic password items) selected automatically on `__APPLE__`.
- [x] Add Linux Secret Service backend via libsecret (D-Bus) as an optional CMake dependency, falling back to env-ref-only when libsecret is not installed.
- [x] Refactor `SecureTokenStore` around an `ISecureTokenBackend` abstraction with a test-only in-memory backend so unit tests never reach the real keychain.
- [x] Add native OAuth PKCE scaffolding behind provider capability flags, including state/verifier generation, authorization URL output, callback URL parsing/validation, one-shot loopback callback capture, authorization-code and refresh-token request construction, curl-backed exchange helpers, token response parsing, and managed AuthSession persistence helpers.
- [x] Add a scriptable callback-to-session OAuth completion bridge through `auth oauth-complete`.
- [x] Implement parameterized provider adapter native login/refresh orchestration for callers that provide callback/token endpoint/client configuration.
- [x] Add Gemini provider-specific OAuth defaults for Google authorization/token endpoints and default scopes.
- [x] Add a single-command OAuth login bridge that orchestrates start/listen/token-exchange/session persistence for providers with OAuth defaults or explicit endpoints.
- [x] Broaden OAuth provider discovery: stub entries for `openai` / `anthropic` / `qwen` with `origin` and `note` metadata, surfaced via `auth oauth-defaults` and `auth oauth-config-validate --all` so users can see at a glance which providers ship builtin defaults vs. require workspace overrides.
- [x] Ship public PKCE endpoints for OpenAI (`auth.openai.com/authorize` + `/oauth/token`) and native `OpenAiAgent` REST adapter targeting `api.openai.com/v1/chat/completions`. Anthropic PKCE remains deferred until stable public endpoints exist.
- [ ] Document and ship public PKCE endpoints for Anthropic once the provider exposes stable customer flows (currently registered as `stub` defaults).
- [x] Polish multi-provider interactive login UX (single-binary CLI prompt with per-provider hints sourced from the new origin/note metadata). `agentos auth login-interactive` now (a) lists registered providers when an unknown `provider=<id>` is given so users can recover, (b) emits a copy-paste-ready `agentos auth oauth-login <provider> client_id=... redirect_uri=... port=... open_browser=true profile=default set_default=true` template when the user picks `browser_oauth`, and (c) annotates `stub`/`config` providers in the redirect message so the user knows whether builtin defaults exist or `runtime/auth_oauth_providers.tsv` overrides are required.
- [x] Update `AUTH_PRD.md`, `AUTH_DESIGN.md`, and README examples after native credential storage and OAuth flows exist.

### Phase K: Plugin Long-Running Lifecycle

- [x] Add manifest lifecycle fields such as `lifecycle_mode=oneshot|persistent`, `startup_timeout_ms`, and `idle_timeout_ms`.
- [x] Split plugin manifest parsing/validation from runtime execution to reduce `plugin_host.cpp` coupling before adding process pools.
- [x] Extract shared PluginSpec support validation and PluginSpec-to-CliSpec conversion into `plugin_spec_utils.*`.
- [x] Extract `PluginSkillInvoker` implementation into `plugin_skill_invoker.cpp`.
- [x] Extract plugin workspace sandbox path containment into `plugin_sandbox.*`.
- [x] Implement a persistent JSON-RPC plugin session manager with request IDs, timeout handling, stderr capture, restart-on-failure, and explicit shutdown.
- [x] Extend `agentos plugins health` with lifecycle-aware startup/round-trip checks.
- [x] Add regression coverage for persistent success, timeout, crash/restart, malformed response, lifecycle event output, and session reuse.

### Phase L: Storage Reliability

- [x] Add a transactional batch-write helper for multi-file runtime updates with prepare/commit/recover markers.
- [x] Extend storage recovery tests to simulate interrupted multi-file writes.
- [x] Define the SQLite migration boundary as an interface decision record before adding a second backend.
- [x] Improve audit reconstruction coverage for non-task retained events and mixed scheduler/task timelines.

### Phase M: Agent Result Normalization

- [x] Define a common normalized agent result schema for `summary`, `artifacts`, `metrics`, `tool_calls`, `model`, and provider-specific metadata.
- [x] Normalize Codex/Gemini/Anthropic/Qwen/local_planner adapter outputs into that schema while preserving raw provider output.
- [x] Teach SubagentManager aggregation to consume normalized fields instead of provider-specific passthrough strings.
- [x] Add tests for mixed-provider aggregation and missing-field fallback.

### Phase N: CLI And Docs Maintenance

- [x] Keep `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`, and `completion_review.md` synchronized after each completed phase.
- [x] Split `main.cpp` command groups once feature churn slows, starting with auth/plugin/storage commands.
- [x] Extract the storage command group from `main.cpp` into `src/cli/storage_commands.*`.
- [x] Extract the schedule command group from `main.cpp` into `src/cli/schedule_commands.*`.
- [x] Extract the subagents command group from `main.cpp` into `src/cli/subagents_commands.*`.
- [x] Extract the agents command from `main.cpp` into `src/cli/agents_commands.*`.
- [x] Extract the cli-specs command group from `main.cpp` into `src/cli/cli_specs_commands.*`.
- [x] Extract the plugins command group from `main.cpp` into `src/cli/plugins_commands.*`.
- [x] Extract the trust command group from `main.cpp` into `src/cli/trust_commands.*`.
- [x] Extract the memory command group from `main.cpp` into `src/cli/memory_commands.*`.
- [x] Extract the auth command group from `main.cpp` into `src/cli/auth_commands.*`.
- [x] Finish splitting `plugin_host.cpp` health and execution/session lifecycle modules after lifecycle interfaces stabilize.
- [x] Collapse `PolicyEngine` constructor overloads into an explicit dependency/options object or a single constructor shape.
- [x] Split `router_components.cpp` into distinct SkillRouter, AgentRouter, and WorkflowRouter implementation files with focused unit coverage.
- [x] Extract shared step execution/lifecycle helpers so AgentLoop and SubagentManager share policy/audit/memory step recording semantics.
- [x] Move lesson-derived policy hints out of `AgentLoop` into a single LessonHintProvider or MemoryManager helper.
- [x] Evaluate replacing ad hoc JSON parsing paths with a structured JSON dependency before adding more schema keywords or provider protocols. **Decision: ADR-JSON-001 (Accepted)** — nlohmann/json is fetched via `FetchContent` and is now the JSON dependency. Production and test call sites were migrated, and `src/utils/json_utils.{hpp,cpp}` has been retired; follow-on shared schema-validator work has started with shared parse-time object-schema validation in `src/core/schema`.


### Phase O: Resident Operation Modes

- [x] Add `agentos interactive` REPL mode with command parsing (run/agents/skills/status/memory/schedule/help/exit).
- [x] Add `agentos serve` HTTP API server mode using cpp-httplib with REST JSON endpoints (health/skills/agents/status/run/schedule/memory).
- [x] Change default no-args behavior from demo to interactive REPL.
- [x] Add cpp-httplib v0.18.3 dependency via CMake FetchContent.
- [x] Integrate Ctrl-C cancellation for both modes via `InstallSignalCancellation()`.
- [x] Add CORS headers for local frontend development.
- [x] Verify 87/87 build targets, 12/12 CTest tests, manual REPL and HTTP API validation.
### Phase A: Documentation Truth And Planning

- [x] Create `plan.md` with completion review and TODO plan.
- [x] Update `docs/ROADMAP.md` to reflect actual current implementation status.
- [x] Add an implementation status table to `README.md` or link this plan from `README.md`.
- [x] Update `docs/ARCHITECTURE.md` directory layout to include `auth`, `scheduler`, `trust`, and `core/orchestration`.
- [x] Add "Known Gaps" sections to stale design docs rather than leaving aspirational text unmarked.

### Phase B: Auth Completion

- [x] Add `auth refresh` command and adapter interface coverage.
- [x] Implement real OAuth refresh token exchange once OAuth sessions exist.
- [x] Add workspace default profile mapping.
- [x] Implement system credential store integration or clearly mark local fallback as insecure/dev-only.
- [x] Implement OAuth PKCE skeleton for Gemini or explicitly defer OAuth from MVP.
- [x] Add tests for logout, status reload, and missing env refs.
- [x] Add CLI session import behavior tests with controllable CLI fixtures.

### Phase C: Scheduler Hardening

- [x] Add `schedule tick` command for foreground due-task execution.
- [x] Add `schedule daemon` mode or service wrapper for long-running background execution.
- [x] Add cron expression support or a documented smaller recurrence grammar.
- [x] Add retry/backoff fields to `ScheduledTask`.
- [x] Record scheduler execution metadata separately from task execution logs.
- [x] Add tests for missed tasks, disabled tasks, failed tasks, and recurring task persistence.
- [x] Add configurable missed-run policy for interval tasks.

### Phase D: Workflow Evolution

- [x] Create a durable `WorkflowStore` skeleton separate from candidate generation.
- [x] Add workflow promotion command, for example `agentos memory promote-workflow <name>`.
- [x] Teach Router to prefer enabled stable workflows by trigger match and score ordering.
- [x] Extend `workflow_run` to execute stored workflow definitions, not only built-in workflows.
- [x] Add required-input workflow applicability checks beyond trigger task type.
- [x] Add `LessonStore` for repeated failure patterns.
- [x] Use lessons as routing hints.
- [x] Use lessons as policy hints.

### Phase E: Agent And Subagent System

- [x] Split Router internals into SkillRouter, AgentRouter, and WorkflowRouter without changing public behavior.
- [x] Add automatic agent candidate selection for SubagentManager.
- [x] Add WorkspaceSession abstraction for multi-agent work.
- [x] Add cost/concurrency limits for parallel subagent execution.
- [x] Add deterministic role assignment and role-scoped subtask objectives.
- [x] Add provider adapters beyond Codex CLI only after auth/session boundaries are clear.

### Phase F: CLI And Plugin Ecosystem

- [x] Add external CLI spec loader from a repo-local spec directory.
- [x] Add `jq_transform` CLI skill or explicitly remove it from Roadmap MVP.
- [x] Add command redaction support for sensitive arguments.
- [x] Add process resource controls where OS support is available.
- [x] Design and implement a minimal Plugin Host manifest and stdio protocol.

### Phase G: Safety And Policy

- [x] Enforce `idempotency_key` for side-effecting skills where replay can duplicate effects.
- [x] Add explicit approval policy hooks for high-risk operations.
- [x] Add role/user-level permission grants on top of the current PermissionModel.
- [x] Add audit coverage for trust/pairing/identity mutations.
- [x] Add secret redaction tests for audit paths.

### Phase H: Storage And Reliability

- [x] Decide whether TSV remains MVP storage or migrate to SQLite.
- [x] Add file locking or single-writer guard for runtime stores.
- [x] Add storage versioning metadata and basic helper plumbing.
- [x] Add explicit storage migration helper for manifest/version normalization.
- [x] Add explicit legacy path migration into the `runtime/` storage layout.
- [x] Add crash-safe writes for allowlist, identities, scheduler tasks, memory stats, and execution cache.
- [x] Add export tooling for runtime state.
- [x] Add import tooling for runtime state.

### Phase I: Testing And CI

- [x] Split smoke tests into module-level unit tests.
- [x] Add CLI integration tests for each command group.
- [x] Add negative-path tests for policy, trust, scheduler, and auth.
- [x] Add GitHub Actions or documented local CI command.
- [x] Add fixtures for cross-platform command execution differences.

## Phase O: Self-Extension And Chat Resilience (2026-04-29)

Driven by user-reported pain: chat path stalls 2 minutes on dead endpoints; "give it a URL to learn a new skill" has no code path; main agent prompt lists skills by name only so the model hallucinates capabilities; `interactive_commands.cpp` has grown to 3,179 lines and now contains dev/research/intent logic that should be skills.

Three orthogonal slices, picked so two agents can work in parallel without touching each other's files.

### Slice O-1: `learn_skill` builtin

- [x] Add `src/skills/builtin/learn_skill.{hpp,cpp}`. Inputs: `name=<id> binary=<exec> args_template=<...> description=<...>` OR `from_url=<spec_url>`. Output: writes `runtime/cli_specs/learned/<name>.tsv`, validates the learned Capability Declaration, calls the shared reload helper, returns `{registered: true, skill_name: ...}`.
- [x] Refactor `ReloadExternalSkills` into `src/cli/skill_reload.{hpp,cpp}` so `learn_skill` can call it without depending on the REPL module.
- [x] Register `learn_skill` in composition root.
- [x] Add `tests/learn_skill_tests.cpp` covering spec write under `cli_specs/learned/`, post-write reload makes the new skill findable via `SkillRegistry::find`, protected/registered name conflicts, and malformed args returning `SchemaValidationFailed`.
- [x] Add CTest entry in `CMakeLists.txt` plus warning stanza coverage through the normal build.
- [x] README "当前实现状态" line for self-extension.

### Slice O-2: Chat resilience (timeout + healthy fallback + richer prompt)

- [x] In `src/cli/interactive_commands.cpp:RunChatPrompt` (line 1439): default `task.timeout_ms` from 120000 to 30000; when result fails with `ExternalProcessFailed` / `Timeout` / `AuthExpired` / `AuthUnavailable`, retry against the next healthy chat-capable agent in priority order (gemini, anthropic, openai, qwen) with a per-agent 30 s ceiling and surface a one-line "fell back to <agent> after <prev> failed: <code>" message.
- [x] In `src/hosts/agents/main_agent.cpp:FormatRegisteredSkills` (line 170): for each skill emit `name | risk | "use when: <hint>"` where hint comes from `SkillManifest.description` first sentence, plus the required-input names parsed from `input_schema_json`. Cap remains `kMaxListed=40`.
- [x] Tests: `tests/chat_fallback_tests.cpp` (mock unhealthy main → verify gemini gets the request); `tests/main_agent_prompt_tests.cpp` (prompt contains "use when:" lines for at least 3 sample skills).
- [x] Two new CTest targets + warning stanzas.

### Slice O-3: Decompose `interactive_commands.cpp`

Status (2026-05-06): **complete for the current slice**. Development/research skills and intent classification are in place, and REPL free-form routing now dispatches those requests through the normal routed task path.

- [x] `src/skills/builtin/development_skill.{hpp,cpp}` created, registered as `development_request`. ~778 LOC, includes acceptance/repair loop, deliverables manifest, agent task dir, event-printer hook.
- [x] `src/skills/builtin/research_skill.{hpp,cpp}` created, registered as `research_request`. ~333 LOC.
- [x] `src/cli/intent_classifier.{hpp,cpp}` created (regex tables + `RouteDecisionExplanation` + `UsageSnapshot` types moved here). ~427 LOC.
- [x] **REPL dispatch into the new modules** — free-form text now records a classifier route decision and dispatches `development_request` / `research_request` through `AgentLoop` when classified accordingly.
- [x] No behavior regression: 17/17 ctest green. The old `runtime/skill_routes.tsv` direction has been replaced by Capability Declaration Route Hint coverage.

### Slice O-4 (follow-up): wire intent_classifier into REPL dispatch

- [x] In `interactive_commands.cpp` REPL loop, after slash-command dispatch, call `ClassifyInteractiveRequest` and route by `InteractiveRouteKind`: `development_agent → loop.run({task_type:"development_request",...})`, `research_agent → loop.run({task_type:"research_request",...})`, otherwise fall through to `RunChatWithFallback`.
- [x] Replace the old `runtime/skill_routes.tsv` test direction with Capability Declaration Route Hint coverage; a separate routes file is no longer the default plan.

### Coordination notes

- O-1 and O-2 are file-disjoint (skill module vs REPL+main_agent edits); they run in parallel.
- O-3 touches `interactive_commands.cpp` heavily and must run **after** O-2's edits to `RunChatPrompt` land, otherwise merge conflicts.
- Composition root (`src/main.cpp` register block) is touched by O-1 and O-3 — coordinate by having O-3 wait for O-1's commit before adding its registrations.

### 2026-05-06 goal-batch follow-up

- [x] Capability Contract tests now explicitly cover input string coercion vs. native JSON Capability Output validation semantics.
- [x] Plugin protocol normalization tests cover stdio JSON output, JSON-RPC `result` extraction, invalid output, schema-invalid output, and `PluginSkillInvoker` mapping from normalized output.
- [x] TSV StorageBackend tests now include POSIX cross-process append concurrency coverage.
- [x] Auth Login Flow fixture-store tests now cover API-key env refs, CLI passthrough, browser OAuth fallback, native PKCE completion, cloud ADC, static refresh fallback, and OAuth refresh.
- [ ] Keep Anthropic PKCE deferred until official stable public customer endpoints exist.
- [ ] Revisit SQLite only if query-heavy workflows, cross-component joins, or multi-process write contention become routine.
- [ ] Create an ADR only when a durable candidate is rejected for a non-obvious reason.

### 2026-05-06 interactive job architecture follow-up

- [x] First Interactive Job Runtime slice: `development_request` and `research_request` share a REPL background job **Module** while preserving the `AgentLoop` **Interface** as the dispatch **Seam**.
- [x] Add a Task Wait Policy **Module** that models `idle_timeout`, `soft_deadline`, and `hard_deadline` instead of one fixed timeout. Keep policy in an **Adapter** behind task execution so simple chat, research, and artifact generation can use different **Depth** without changing skill **Implementation** code.
- [x] Add a REPL exit first slice: `exit` reports running jobs and returns to the prompt; `exit --wait` keeps the old explicit wait behavior. Full detach/cancel remains blocked until background jobs no longer capture REPL-local runtime references.
- [x] Continue candidate 3 first slice: `AgentEventRuntimeStore` now owns task-dir lookup and latest `status.json` summary formatting for REPL jobs. Follow-up remains to move event/status writes from individual skill **Implementation** files behind the same durable **Interface**.
- [x] Continue candidate 4 first slice: route decisions now carry execution mode metadata (`sync` or `async_job`) and persist it in routing JSON, making Interactive Route Action an explicit **Seam**. Follow-up remains to add queue priority and budget hints after the job store shape is settled.

## Immediate Next TODO

- [x] Sync `docs/ROADMAP.md` with this plan.
- [x] Link `plan.md` from `README.md`.
- [x] Add audit events for `trust identity-*`, `trust pair`, `trust block`, and `trust remove`.
- [x] Add `schedule tick` command.
- [x] Record scheduler execution metadata separately from task execution logs.
- [x] Add retry/backoff fields to `ScheduledTask`.
- [x] Add cron expression support or a documented smaller recurrence grammar.
- [x] Add tests for missed tasks and disabled tasks.
- [x] Add `WorkflowStore` skeleton.
- [x] Add workflow promotion command.
- [x] Extend `workflow_run` to execute stored workflow definitions.
- [x] Teach Router to prefer promoted workflows when applicable.
- [x] Add richer workflow applicability conditions beyond trigger task type.
- [x] Add `LessonStore` skeleton.
- [x] Use lessons as routing hints.
- [x] Use lessons as policy hints.
- [x] Add `auth refresh` command and adapter interface coverage.
- [x] Add workspace default profile mapping.
- [x] Implement system credential store integration or clearly mark local fallback as insecure/dev-only.
- [x] Implement OAuth PKCE skeleton for Gemini or explicitly defer OAuth from MVP.
- [x] Add tests for logout, status reload, and missing env refs.
- [x] Add CLI session import behavior tests with controllable CLI fixtures.

## Post-Review Work

Tracking items raised by `completion_review.md` that fall outside the original
Phase A–I plan. These are research / decision items rather than direct
implementation tasks.

- [x] **Structured JSON dependency** — Decision is now **Accepted** in
  [`docs/ADR-JSON-001.md`](docs/ADR-JSON-001.md) with **Phase 1 partial**
  delivered: nlohmann/json fetched via CMake `FetchContent`, migrated paths
  in `json_utils.cpp::NumberAsJson`, `subagent_manager.cpp::ExtractPlanActions`,
  `provider_adapters.cpp` CLI probe parsing, the four V2 streaming agent
  adapters' SSE/NDJSON pipelines, and `workflow_run_skill.cpp` (which closes
  the ADR §1.2 double-escaping bug). Later slices have also migrated
  `audit_logger`, all builtin skills, the CLI skill invoker, local planner,
  OAuth token-exchange parsing, schema-column parse validation, diagnostics
  JSON output, agent result normalization, Plugin JSON manifest parsing,
  Plugin JSON-RPC request / response helpers, and Plugin output schema
  validation. Phase 5 is now complete: remaining test fixtures were migrated,
  stale include-only references were removed, and `src/utils/json_utils.{hpp,cpp}`
  was deleted from the build.
- [x] **Storage backend boundary** — already tracked under Phase H; the
  long-form decision is now present in [`docs/ADR-STORAGE-001.md`](docs/ADR-STORAGE-001.md)
  (TSV remains MVP, SQLite deferred until a `StorageBackend` boundary is
  needed).

## Progress Log

- 2026-04-23: Reviewed current code/docs completion and created this plan.
- 2026-04-23: Synced `docs/ROADMAP.md` to actual implementation status and linked `plan.md` from `README.md`.
- 2026-04-23: Updated `docs/ARCHITECTURE.md` directory layout and added current implementation gaps to auth docs.
- 2026-04-23: Added trust mutation audit events and verified `trust pair` writes audit records.
- 2026-04-23: Added `schedule tick` foreground loop and verified it executes a due write task.
- 2026-04-23: Added durable `WorkflowStore` skeleton backed by `runtime/memory/workflows.tsv` and smoke-tested persistence.
- 2026-04-23: Added `memory promote-workflow` and verified candidate promotion into `workflows.tsv`.
- 2026-04-23: Extended `workflow_run` to execute promoted WorkflowStore definitions within declared workflow permissions.
- 2026-04-23: Taught Router to prefer enabled promoted workflows for matching task types and verified automatic workflow routing.
- 2026-04-23: Added `required_inputs` workflow applicability checks and verified Router skips workflows when inputs are missing.
- 2026-04-23: Added `LessonStore` backed by `runtime/memory/lessons.tsv` and verified repeated failure aggregation.
- 2026-04-23: Used LessonStore as Router hints to suppress repeated workflow failures and penalize repeatedly failing agents.
- 2026-04-23: Added LessonStore policy hints for repeated PolicyDenied results without changing hard policy decisions.
- 2026-04-23: Added `auth refresh` command, AuthManager refresh flow, adapter coverage, and unsupported-refresh handling.
- 2026-04-23: Added workspace auth default profile mapping backed by `runtime/auth_profiles.tsv`.
- 2026-04-23: Added `auth credential-store` and SecureTokenStore status to explicitly mark the env-ref-only dev fallback.
- 2026-04-25: Added Windows Credential Manager managed-token backend to SecureTokenStore, with read/write/delete/status coverage while preserving env-ref support.
- 2026-04-25: Added reusable OAuth PKCE start/callback scaffolding with S256 challenge generation, `auth oauth-start` authorization URL construction, callback URL parsing, one-shot localhost listener via `auth oauth-listen`, state validation, authorization-code and refresh-token request form-body construction, curl-backed exchange helpers, token response parsing, managed AuthSession persistence helpers, `auth oauth-complete`, parameterized provider adapter native OAuth login/refresh completion, `auth oauth-callback`, `auth oauth-token-request`, `auth oauth-refresh-request`, and RFC 7636 regression coverage.
- 2026-04-25: Added Gemini Google OAuth default authorization/token endpoints and default scopes for `oauth-start`, `oauth-complete`, and parameterized browser OAuth adapter completion, with Auth and CLI integration coverage.
- 2026-04-26: Added optional system-browser launch for `auth oauth-start` through `open_browser=true` while keeping the default scriptable output unchanged.
- 2026-04-26: Added `auth oauth-defaults [provider]` for scriptable provider OAuth defaults discovery, including unsupported-provider reporting.
- 2026-04-26: Added `auth oauth-login` as a single-command PKCE OAuth bridge that creates the authorization URL, optionally opens the browser, validates an explicit callback URL or captures one loopback callback, exchanges the code, persists the managed session, and supports deterministic state/verifier inputs for automation tests.
- 2026-04-26: Added repo-local OAuth defaults overrides via `runtime/auth_oauth_providers.tsv`, consumed by `auth oauth-defaults`, `auth oauth-start`, `auth oauth-login`, and `auth oauth-complete`, plus `auth oauth-config-validate` diagnostics.
- 2026-04-26: Added `auth profiles [provider]` to list persisted auth sessions with default-profile markers as a foundation for multi-account UX.
- 2026-04-26: Added `set_default=true` to login and OAuth completion commands so a successful profile creation/import can update provider defaults in the same command.
- 2026-04-23: Added explicit BrowserOAuthUnavailable handling for Gemini browser-OAuth passthrough when no Gemini CLI OAuth session can be imported.
- 2026-04-23: Added Auth tests for logout, SessionStore reload status, and missing environment credential refs.
- 2026-04-23: Added scheduler run history metadata backed by `runtime/scheduler/runs.tsv` and `schedule history`.
- 2026-04-23: Added scheduler retry/backoff fields and verified failed tasks retry before disabling.
- 2026-04-23: Added small scheduler recurrence grammar `every:<n>s|m|h|d` as an MVP alternative to full cron.
- 2026-04-25: Added five-field scheduler cron support with validation, next-run computation, persistence, CLI integration, and run/reschedule coverage.
- 2026-04-25: Added scheduler cron aliases `@hourly`, `@daily`, `@weekly`, and `@monthly` with validation, next-run computation, persistence, CLI integration, and run/reschedule coverage.
- 2026-04-25: Added scheduler `@yearly` / `@annually` aliases and cron day-of-month / day-of-week OR semantics with deterministic calendar regression coverage.
- 2026-04-23: Added scheduler regression coverage for disabled tasks and missed interval tasks.
- 2026-04-23: Added controllable Codex/Claude CLI session fixture tests for auth probe/import success and unavailable-session failure.
- 2026-04-23: Added `schedule daemon` foreground loop as the long-running scheduler wrapper, reusing the `tick` execution path.
- 2026-04-23: Added `missed_run_policy=run-once|skip` for interval tasks and covered skip semantics.
- 2026-04-23: Split Router internals into SkillRouter, AgentRouter, and WorkflowRouter while preserving public selection behavior.
- 2026-04-23: Added automatic SubagentManager candidate selection by healthy capability match, historical stats, and lessons.
- 2026-04-23: Added WorkspaceSession abstraction for opening, using, and closing session-capable agent adapters within a workspace.
- 2026-04-23: Added SubagentManager parallel concurrency limits and estimated-cost budget checks with memory cost stats.
- 2026-04-25: Added deterministic SubagentManager role assignment via inferred capabilities or `roles=agent:role`, role-scoped subtask objectives, role summaries in output, and CLI/unit coverage.
- 2026-04-23: Added repo-local external CLI spec loading from `runtime/cli_specs/*.tsv` and verified dynamic skill registration.
- 2026-04-24: Made the external CLI spec TSV parser preserve empty fields so optional empty columns no longer shift later fields, with regression coverage.
- 2026-04-24: Added `agentos cli-specs validate` and external CLI spec load diagnostics for scriptable validation of repo-local CLI specs.
- 2026-04-23: Added built-in `jq_transform` CLI skill backed by jq and covered it with a controllable CLI fixture.
- 2026-04-23: Rechecked docs task completion against code/tests and corrected stale Roadmap, architecture, agent-system, and audit-status entries.
- 2026-04-23: Added CLI sensitive argument redaction for command display/stdout/stderr and covered it with a smoke test.
- 2026-04-23: Shared sensitive-value redaction between CLI and AuditLogger and added audit log redaction coverage.
- 2026-04-23: Enforced `idempotency_key` for non-idempotent filesystem-write skills and made Scheduler assign per-run keys automatically.
- 2026-04-23: Added explicit `approval_id` hooks for high-risk and unknown-risk skill/agent policy decisions.
- 2026-04-23: Added per-task `permission_grants` enforcement for Skill permissions and `agent.invoke`, with CLI/Scheduler/Subagent option support.
- 2026-04-25: Added persistent `RoleCatalog` backed by `runtime/trust/roles.tsv`, role/user assignment and removal CLI commands, automatic user-assignment cleanup when roles are removed, and PolicyEngine fallback to user-role permissions when per-task `permission_grants` are absent.
- 2026-04-25: Added persisted pairing invites backed by `runtime/trust/invites.tsv`, with `trust invite-create`, `trust invite-accept`, active invite listing, TTL validation, one-time consumption, permission validation, audit coverage, manifest registration, and CLI/unit tests.
- 2026-04-25: Added paired-device lifecycle metadata to `runtime/trust/allowlist.tsv`, plus `trust device-label`, `trust device-seen`, and `trust unblock` commands with audit and CLI/unit coverage.
- 2026-04-26: Added `trust device-show` for scriptable paired-device inspection with lifecycle metadata, explicit not-found diagnostics, and CLI integration coverage.
- 2026-04-25: Added persistent `ApprovalStore` backed by `runtime/trust/approvals.tsv`, CLI approval request/approve/revoke/list commands, storage manifest registration, PolicyEngine approved-state enforcement for high-risk CLI runtime execution, and CLI/unit coverage.
- 2026-04-23: Added Plugin Host with repo-local plugin specs, PluginSkillInvoker, one-shot stdio-json-v0 execution, and smoke coverage.
- 2026-04-25: Added repo-local JSON plugin manifests in `runtime/plugin_specs/*.json`, reusing plugin.v1 validation, diagnostics, duplicate detection, and one-shot stdio-json-v0 execution.
- 2026-04-25: Added optional plugin `health_args_template` / `health_timeout_ms` probes for TSV and JSON manifests, wired into `agentos plugins health` with success and failure coverage.
- 2026-04-25: Added plugin manifest diagnostics rejecting `health_args_template` placeholders that require runtime input arguments, with TSV and JSON regression coverage.
- 2026-04-25: Added plugin manifest `sandbox_mode=workspace|none` parsing, diagnostics, CLI listing, and runtime workspace path-argument containment.
- 2026-04-25: Added plugin manifest lifecycle fields `lifecycle_mode=oneshot|persistent`, `startup_timeout_ms`, and `idle_timeout_ms` for TSV/JSON manifests, CLI listing, validation, and regression coverage; `persistent` is currently restricted to `json-rpc-v0` while the reusable session manager remains open.
- 2026-04-26: Added runtime guardrails for `lifecycle_mode=persistent`: health and execution now report `PluginLifecycleUnavailable` instead of silently falling back to one-shot execution, and plugin skill output/capabilities include lifecycle metadata.
- 2026-04-26: Implemented persistent `json-rpc-v0` plugin sessions with stdin/stdout JSON-RPC round-trips, request IDs, timeout handling, stderr capture, failed-session eviction/restart, destructor shutdown, lifecycle-aware health round-trips, and persistent success/timeout/malformed-response regression coverage.
- 2026-04-26: Added persistent plugin lifecycle observability via `lifecycle_event` (`started`, `reused`, `restarted`, eviction variants) in run/skill results, plus crash-after-response restart coverage.
- 2026-04-26: Added `PluginHost` active-session counting, manual session close, `idle_timeout_ms` restart behavior, and regression coverage for session close and idle restart.
- 2026-04-26: Added `PluginHostOptions.max_persistent_sessions` with LRU eviction for persistent JSON-RPC plugin sessions and regression coverage for pool eviction/restart behavior.
- 2026-04-26: Added `agentos plugins inspect name=<plugin> [health=true]` for scriptable manifest inspection plus optional single-plugin health probes, including protocol, permissions, arguments, resource limits, sandbox, lifecycle, source metadata, validity, command availability, and missing-plugin diagnostics.
- 2026-04-26: Added `agentos plugins lifecycle` for scriptable manifest-level lifecycle summaries, including oneshot/persistent counts and default persistent session cap.
- 2026-04-26: Added workspace-configurable persistent plugin session caps through `runtime/plugin_host.tsv`, registered that file in `runtime/storage_manifest.tsv`, and surfaced invalid plugin host config diagnostics through `agentos plugins lifecycle`.
- 2026-04-26: Added per-plugin `PluginSpec.pool_size` cap with LRU eviction scoped to the same plugin name (capped by global `max_persistent_sessions`), TSV/JSON manifest parsing, and `agentos plugins lifecycle` / `plugins inspect` `pool_size` reporting.
- 2026-04-26: Added runtime plugin session admin commands `agentos plugins sessions`, `agentos plugins session-restart name=<plugin>`, and `agentos plugins session-close name=<plugin>`, exposing `PluginHost::list_sessions/close_sessions_for_plugin/restart_sessions_for_plugin`, plus pool/admin and CLI integration regression coverage.
- 2026-04-26: Added `trust role-show`, `trust user-role-show`, and `trust approval-show` for scriptable single-record Trust/Policy admin inspection with not-found diagnostics and CLI integration coverage.
- 2026-04-25: Extended `agentos plugins`, `agentos plugins validate`, and `agentos plugins health` loaded-spec output with source file and line metadata.
- 2026-04-25: Required plugin manifests to declare `process.spawn`, moving missing process-spawn permission from implicit runtime behavior into manifest diagnostics.
- 2026-04-25: Enforced JSON-object-shaped stdout for successful `stdio-json-v0` plugin runs and added invalid-output regression coverage.
- 2026-04-25: Added `json-rpc-v0` plugin protocol support with JSON-RPC 2.0 response validation, result-object extraction into `plugin_output`, output-schema validation against `result`, and error-response rejection coverage.
- 2026-04-25: Added structured `plugin_output` embedding to PluginSkillInvoker results while retaining raw stdout/stderr metadata.
- 2026-04-25: Added `output_schema_json.required` validation for successful `stdio-json-v0` plugin output.
- 2026-04-25: Added `output_schema_json.properties.*.type` validation for successful `stdio-json-v0` plugin output.
- 2026-04-25: Added string `const` and `enum` validation for successful `stdio-json-v0` plugin output.
- 2026-04-25: Added string `minLength`, `maxLength`, and `pattern` validation for successful `stdio-json-v0` plugin output.
- 2026-04-25: Added numeric `minimum`, `maximum`, `exclusiveMinimum`, `exclusiveMaximum`, and `multipleOf` validation for successful `stdio-json-v0` plugin output.
- 2026-04-25: Added `output_schema_json.additionalProperties:false` validation for successful `stdio-json-v0` plugin output.
- 2026-04-25: Added `output_schema_json.allOf` / `anyOf` / `oneOf` required-branch validation for successful plugin output.
- 2026-04-25: Added `output_schema_json.propertyNames`, `minProperties`, and `maxProperties` validation for successful plugin output.
- 2026-04-25: Added `output_schema_json.dependentRequired`, legacy array-valued `dependencies`, and object-level `not.required` validation for successful plugin output.
- 2026-04-25: Added basic `output_schema_json.if` / `then` / `else` required-branch validation for successful plugin output.
- 2026-04-23: Added plugin.v1 manifest version and unsupported plugin protocol/version filtering.
- 2026-04-23: Added shared plugin spec health validation for loader, runtime rejection, and PluginSkillInvoker health.
- 2026-04-24: Added structured plugin health reporting and an `agentos plugins` command with CLI integration coverage.
- 2026-04-24: Added `agentos plugins health` for scriptable plugin health checks with failing exit status for unhealthy loaded plugins.
- 2026-04-24: Added plugin spec load diagnostics so skipped plugin specs report source file, line number, and reason through the loader and plugin health CLI.
- 2026-04-24: Added `agentos plugins validate` for scriptable plugin manifest validation that reports skipped-spec diagnostics without requiring plugin binaries to be installed.
- 2026-04-24: Added startup `config_diagnostic` audit events for skipped external CLI specs and plugin specs so invalid repo-local capability declarations are visible outside validation commands.
- 2026-04-24: Tightened external CLI and plugin spec parsing so invalid numeric/resource fields, plus plugin boolean fields, become skipped-spec diagnostics instead of silently falling back to defaults.
- 2026-04-24: Added spec validation bounds so external CLI and plugin `timeout_ms` must be positive and integer resource-limit fields must be non-negative, preventing negative timeout/resource settings from reaching process execution.
- 2026-04-24: Added external CLI and plugin spec diagnostics for `required_args` that are not referenced by `args_template` placeholders, while allowing reserved `cwd` to be required.
- 2026-04-24: Added lightweight external CLI and plugin spec diagnostics for `input_schema_json` / `output_schema_json` values that are not JSON-object-shaped, avoiding silent schema validation disablement from malformed schema fields.
- 2026-04-24: Added external CLI `parse_mode` whitelist diagnostics so repo-local CLI specs must declare one of `text`, `json`, or `json_lines`.
- 2026-04-24: Added external CLI and plugin spec diagnostics for unsupported `risk_level` values and unknown permissions, moving repo-local capability policy mistakes from runtime denial to validation.
- 2026-04-24: Added duplicate-name diagnostics for external CLI and plugin specs so later repo-local declarations no longer silently overwrite earlier registered skills.
- 2026-04-24: Preserved source file/line metadata on loaded external CLI and plugin specs and skipped startup registration when a repo-local spec name conflicts with an already registered built-in skill, recording the conflict in audit.
- 2026-04-24: Extended `agentos cli-specs validate`, `agentos plugins validate`, and `agentos plugins health` to report name conflicts against built-in skills and external CLI specs without falsely flagging already-registered valid specs as self-conflicts.
- 2026-04-25: Added CLI integration regression coverage proving plugin specs that share a name with a valid external CLI spec fail `plugins validate` and are audited during startup.
- 2026-04-25: Extended CLI/plugin validate output with `conflicting_cli_spec` / `conflicting_plugin` source file and line diagnostics for registered-skill name conflicts.
- 2026-04-25: Extracted shared external spec parsing helpers for TSV splitting, strict numeric parsing, JSON-object shape checks, and string joining to keep CLI and Plugin loaders consistent.
- 2026-04-25: Added `agentos_spec_parsing_tests` for direct coverage of shared spec parsing helpers and increased local verification to 10 CTest targets.
- 2026-04-25: Added authenticated Gemini `generateContent` agent adapter backed by existing auth sessions/default profiles and `CliHost` curl execution, plus `agentos_agent_provider_tests`; local verification now passes 11 CTest targets.
- 2026-04-25: Added Gemini CLI browser-OAuth passthrough import (`auth login gemini mode=browser_oauth`) and routed external Gemini sessions through `gemini -p`, plus empty-stdin CLI execution to avoid headless Gemini hangs.
- 2026-04-25: Added Gemini `cloud_adc` login via Google Application Default Credentials plus `gcloud auth application-default print-access-token`, and routed Gemini REST calls through the minted bearer token.
- 2026-04-25: Added model-selection propagation from `agentos run ... model=<name>` into agent constraints, covered Gemini CLI OAuth routing with `gemini-3.1-pro` shorthand normalized to the official `gemini-3.1-pro-preview` model id, and verified a real Gemini 3.1 Pro Preview smoke through the imported CLI OAuth session.
- 2026-04-25: Added `qwen` agent adapter backed by Qwen API-key auth sessions and Alibaba Cloud Model Studio OpenAI-compatible Chat Completions, with endpoint/auth/model/summary/redaction coverage in `agentos_agent_provider_tests`.
- 2026-04-25: Normalized agent structured output with a top-level `content` field for Codex CLI, Gemini, Anthropic, and Qwen provider adapters.
- 2026-04-25: Replaced the production `mock_planner` naming/path with `local_planner`, a local deterministic planning adapter that tracks sessions, rejects closed sessions, emits structured plan steps, and records pre-execution cancellation.
- 2026-04-25: Added deterministic per-agent subtask objectives for SubagentManager through `subtasks=role_or_agent=objective;...` and `subtask_<agent|role>=...`, with orchestration regression coverage.
- 2026-04-25: Added `auto_decompose=true` SubagentManager path that invokes a decomposition-capable planner, extracts `plan_steps[].action`, maps generated actions into deterministic subtask objectives, exposes `decomposition_agent` in output, and covers unit/CLI flows.
- 2026-04-25: Added subagent step-level structured output retention and top-level `agent_outputs` aggregation so provider fields such as content/model/profile/auth_source survive orchestration beyond plain summaries.
- 2026-04-24: Made the plugin TSV manifest parser preserve empty fields so optional empty columns no longer shift later fields, with regression coverage.
- 2026-04-23: Added required input schema validation before skill execution with smoke coverage.
- 2026-04-24: Extended skill input schema validation to check basic `properties.*.type` scalar types (`string`, `number`, `integer`, `boolean`) before skill execution, with regression coverage.
- 2026-04-24: Extended skill input schema validation to check `enum`, `minLength`, `maxLength`, `minimum`, and `maximum` constraints before skill execution, with regression coverage.
- 2026-04-24: Extended skill input schema validation to check string `pattern` constraints before skill execution, with regression coverage.
- 2026-04-24: Extended skill input schema validation to check numeric `multipleOf` constraints before skill execution, with regression coverage.
- 2026-04-24: Extended skill input schema validation to check numeric `exclusiveMinimum` and `exclusiveMaximum` constraints before skill execution, with regression coverage.
- 2026-04-24: Extended skill input schema validation to check string `const` constraints before skill execution, with regression coverage.
- 2026-04-24: Extended skill input schema validation to reject undeclared inputs when `additionalProperties:false` is declared, with regression coverage.
- 2026-04-25: Extended skill input schema validation to check `dependentRequired` and legacy array-valued `dependencies` before skill execution, with regression coverage.
- 2026-04-25: Extended skill input schema validation to check object-level `minProperties` and `maxProperties` before skill execution, with regression coverage.
- 2026-04-25: Extended skill input schema validation to check object-level `not.required` before skill execution, with regression coverage.
- 2026-04-25: Extended skill input schema validation to check `propertyNames` constraints before skill execution, including pattern and length coverage.
- 2026-04-25: Extended skill input schema validation to check basic `if` / `then` / `else` conditional required fields before skill execution, with regression coverage.
- 2026-04-25: Extended skill input schema validation to check object-level `allOf` / `anyOf` / `oneOf` required-branch constraints before skill execution, with regression coverage.
- 2026-04-23: Added atomic file replacement for full-file TSV stores and covered the helper with a smoke test.
- 2026-04-23: Added a single-writer lock guard to atomic full-file TSV writes.
- 2026-04-23: Added `runtime/storage_manifest.tsv` metadata with a storage status command and smoke coverage.
- 2026-04-23: Added explicit `storage migrate` support for manifest/version normalization with smoke coverage.
- 2026-04-23: Added manifest-based runtime state export and smoke coverage.
- 2026-04-23: Added manifest-based runtime state import and smoke coverage.
- 2026-04-23: Added locked append-only file helper and moved audit/task/step/scheduler history appends onto it.
- 2026-04-25: Added append-intent recovery for append-only logs so interrupted writes can be replayed on the next append without duplicating already-applied lines.
- 2026-04-23: Extended storage compaction to include malformed-line cleanup for `audit.log`.
- 2026-04-24: Recorded an explicit TSV MVP storage decision in code/docs and surfaced it through `storage status`.
- 2026-04-25: Extended `storage status` with per-component file existence, byte size, and line-count inspection for runtime diagnostics.
- 2026-04-26: Added transactional multi-file storage writes with prepare/commit/recover markers, `storage recover`, and recovery coverage for committed replay plus uncommitted rollback.
- 2026-04-26: Hardened `storage recover` so corrupt committed transaction state is counted via `failed=`, cleaned up, and does not block later valid committed replays or partially apply missing staged payloads.
- 2026-04-26: Added same-process concurrent writer coverage for append logs and atomic replace storage helpers.
- 2026-04-26: Expanded the storage policy into decision record `ADR-STORAGE-001`, surfaced through `storage status`, with deferred SQLite target, StorageBackend migration boundary, required backend capabilities, and TSV import compatibility contract.
- 2026-04-26: Added `storage verify [src=<directory>] [strict=true]` for scriptable manifest-managed file completeness checks before backup/export/import workflows.
- 2026-04-26: Added pre-import backups for `storage import`, with `backed_up_files` and `backup` path reporting for rollback visibility.
- 2026-04-26: Added `storage backups` to list pre-import backup directories with file/byte counts and an aggregate summary.
- 2026-04-26: Added `storage restore-backup name=<backup_name>` to restore pre-import backups while creating a fresh backup of overwritten current files.
- 2026-04-26: Added mixed audit recovery coverage for global events, task-scoped preserved policy events, orphan task events, scheduler_run replacement, and untimed global events; audit recovery now merges individual timed task lifecycle lines with scheduler chunks instead of emitting each recovered task as an indivisible bundle.
- 2026-04-26: Added normalized `agent_result.v1` output for Codex CLI, Gemini, Anthropic, Qwen, and local_planner adapters, plus SubagentManager aggregation of normalized summary/content/model/artifacts/metrics/tool_calls/provider metadata while preserving raw provider output.
- 2026-04-26: Validated the external Opus completion/design review, documented the scriptable-OAuth and idempotency-key semantics more explicitly, and added auto_decompose negative-path coverage for planner failure and invalid planner output.
- 2026-04-26: Added task fingerprint matching to ExecutionCache so idempotency replays require both `idempotency_key` and matching task inputs/context, while retaining legacy cache read compatibility.
- 2026-04-26: Synchronized `completion_review.md` with the completed CLI/plugin refactors, persistent plugin lifecycle, OAuth refresh implementation, and ExecutionCache fingerprint semantics.
- 2026-04-26: Moved lesson-derived PolicyDenied hints into shared `memory/lesson_hints` and reused them from AgentLoop and SubagentManager policy paths.
- 2026-04-26: Split the combined router component implementation into separate `skill_router.cpp`, `agent_router.cpp`, and `workflow_router.cpp` files while preserving existing router behavior.
- 2026-04-26: Replaced PolicyEngine constructor overloads with a single `PolicyEngineDependencies` constructor and migrated runtime/tests to the explicit dependency object.
- 2026-04-27: Restored the missing storage backend decision record as `docs/ADR-STORAGE-001.md`, matching the existing `CurrentStoragePolicy()` contract, storage status output, deferred SQLite target, required backend capabilities, and TSV import compatibility policy; marked the Post-Review storage backend boundary item complete.
- 2026-04-27: Added scheduler DST regression coverage for `America/New_York` cron evaluation: spring-forward gap times now have explicit coverage for running at the first valid instant after the skipped hour, and fall-back fold times have coverage for choosing the first occurrence only and skipping the repeated wall-clock occurrence on the next calculation.
- 2026-04-27: Replaced the persistent plugin health TODO with an explicit JSON-RPC `$/healthz` probe path. `CheckPluginHealth` now sends `$/healthz` instead of invoking the plugin's runtime method with empty params, and `agentos_cli_plugin_tests` includes a persistent fixture that only reports healthy for the health method.
- 2026-04-27: Added effective persistent plugin pool policy reporting to `agentos plugins lifecycle` and `agentos plugins inspect`: both now surface the `effective_pool_size` after applying `max_persistent_sessions` to manifest `pool_size`, with CLI integration coverage for a `pool_size=5` plugin capped by `max_persistent_sessions=3`.
- 2026-04-27: Aligned `agentos plugins inspect` with `plugins lifecycle` for plugin host config diagnostics. Inspect now uses the diagnostic-aware `LoadPluginHostOptionsWithDiagnostics`, prints `plugin_host_config_diagnostic` rows, and exits non-zero when `runtime/plugin_host.tsv` contains an invalid persistent pool policy instead of silently reporting default caps.
- 2026-04-27: Added plugin isolation-profile reporting to `agentos plugins`, `agentos plugins lifecycle`, and `agentos plugins inspect`. The CLI now labels the current isolation boundary (`workspace-paths`, `process-resource-limits`, a combined profile, or `none`) so operators can distinguish path-argument containment/resource limits from a future full OS sandbox.
- 2026-04-27: Added explicit `resource_limits_configured=true|false` reporting beside plugin `isolation_profile` in `agentos plugins`, `plugins lifecycle`, and `plugins inspect`; CLI integration now covers a persistent plugin with `memory_limit_bytes` set, producing the combined `workspace-paths+process-resource-limits` profile.
- 2026-04-27: Added scriptable OAuth provider `endpoint_status=available|deferred|missing` reporting to `auth oauth-defaults` and `auth oauth-config-validate --all`, making supported endpoints, stubbed provider deferrals, and missing config states machine-readable. CLI integration now covers available Gemini/configured endpoints and deferred OpenAI/Anthropic/Qwen stubs.
- 2026-04-27: Documented OAuth provider endpoint status semantics in CLI usage, README, AUTH_PRD, and AUTH_DESIGN. The docs now explicitly state that OpenAI / Anthropic / Qwen remain `endpoint_status=deferred` stubs until stable public PKCE endpoints exist, and that workspace `runtime/auth_oauth_providers.tsv` overrides can move a provider to `available`.
- 2026-04-27: Added negative-path CLI coverage for `endpoint_status=missing`: `auth oauth-config-validate --all` now has a regression assertion for an incomplete workspace OAuth override that supplies an authorization endpoint without a token endpoint.
- 2026-04-27: Added structured `oauth_input_error` diagnostics for `auth oauth-start` and `auth oauth-login` missing-input failures. The diagnostic reports `missing_fields`, provider origin, endpoint_status, and the workspace config path, with CLI integration coverage for missing `redirect_uri` and explicit empty `token_endpoint`.
- 2026-04-27: Extended structured `oauth_input_error` diagnostics to `auth oauth-complete`, including provider registration validation and CLI integration coverage for missing `callback_url` plus explicit empty `token_endpoint`.
- 2026-04-27: Added structured `oauth_input_error` diagnostics to low-level `auth oauth-token-request` and `auth oauth-refresh-request` missing-input failures, preserving the existing first-field human error while adding complete `missing_fields` coverage in CLI integration tests.
- 2026-04-27: Added structured `oauth_input_error` diagnostics to `auth oauth-callback` and `auth oauth-listen` missing-input failures, preserving their first missing-field messages while adding complete `missing_fields` CLI integration coverage.
- 2026-04-27: Added `matched=true|false` to `agentos plugins session-close` and `agentos plugins session-restart` outputs so plugin session admin automation can distinguish no-op commands from commands that affected live sessions. CLI integration covers the unknown-plugin no-op path.
- 2026-04-27: Added in-process CLI-handler coverage for live persistent plugin session admin commands: `RunPluginsCommand` now has regression coverage proving `plugins session-restart` and `plugins session-close` emit `matched=true` when they affect active JSON-RPC plugin sessions.
- 2026-04-27: Added optional `name=<plugin>` filtering to `agentos plugins sessions`. The filtered summary reports `matched=true|false` while preserving the unfiltered total/active output shape, giving plugin session admin scripts the same no-op detection style as `session-close` and `session-restart`. In-process plugin CLI coverage now proves filtered live-session and no-match summaries.
- 2026-04-27: Added explicit `scope=process persistence=none` fields to `agentos plugins sessions` summaries, making the current in-memory/per-CLI-process session admin boundary machine-readable. CLI integration and in-process plugin CLI coverage now assert the scope fields for empty, filtered-match, and filtered-no-match session summaries.
- 2026-04-27: Added `idle_timeout_ms` / `idle_expired` per-session diagnostics plus `idle_expired=<n>` and `dead=<n>` summary counts to `agentos plugins sessions`, giving plugin admin scripts a direct signal for sessions that should be restarted or closed. CLI integration and in-process plugin CLI tests now assert empty, filtered-match, and filtered-no-match diagnostics.
- 2026-04-27: Added `agentos plugins session-prune [name=<plugin>]` to remove idle-expired or dead persistent plugin sessions from the current process. The command reports `pruned=<n>`, `matched=true|false`, `reason=idle_expired_or_dead`, and process-scope fields; CLI integration covers fresh-workspace no-op output and in-process plugin CLI coverage proves filtered idle-expired pruning leaves healthy sessions intact.
- 2026-04-27: Added `dry_run=true` preview support to `agentos plugins session-prune`. Dry runs report `would_prune=<n>` with `pruned=0` and leave sessions untouched, giving automation a safe preflight path before cleaning idle-expired/dead persistent plugin sessions. CLI integration covers fresh-workspace dry-run output and in-process plugin CLI tests prove idle-expired sessions remain active until a real prune.
- 2026-04-27: Added an explicit plugin session admin scope boundary. `agentos plugins sessions`, `session-prune`, `session-restart`, and `session-close` accept/default to `scope=process`; unsupported scopes such as `scope=daemon` now fail with `plugin_sessions_unavailable scope=daemon supported_scope=process` so automation cannot mistake current-process session state for cross-process daemon state.
- 2026-04-26: Added shared `core/execution/task_lifecycle` helpers and routed AgentLoop/SubagentManager step recording plus task finalization through them.
- 2026-04-26: Extracted the `agentos storage` command group from `main.cpp` into `src/cli/storage_commands.*`, preserving storage status/migrate/export/import/recover/compact behavior.
- 2026-04-26: Extracted the `agentos schedule` command group from `main.cpp` into `src/cli/schedule_commands.*`, preserving add/list/history/run-due/tick/daemon behavior.
- 2026-04-26: Extracted the `agentos subagents` command group from `main.cpp` into `src/cli/subagents_commands.*`, preserving explicit/automatic/auto-decompose subagent execution behavior.
- 2026-04-26: Extracted the `agentos agents` listing command from `main.cpp` into `src/cli/agents_commands.*`.
- 2026-04-26: Extracted the `agentos cli-specs` command group from `main.cpp` into `src/cli/cli_specs_commands.*`, preserving external CLI spec listing, validation, skipped-spec diagnostics, and registered-skill conflict diagnostics.
- 2026-04-26: Extracted the `agentos plugins` command group from `main.cpp` into `src/cli/plugins_commands.*`, preserving plugin listing, validation, health checks, skipped-spec diagnostics, and external CLI/builtin name-conflict diagnostics.
- 2026-04-26: Extracted the `agentos trust` command group from `main.cpp` into `src/cli/trust_commands.*`, preserving identity, invite, pairing, role, approval, and device lifecycle commands plus audit events.
- 2026-04-26: Extracted the `agentos memory` command group from `main.cpp` into `src/cli/memory_commands.*`, preserving memory summary/stats, lessons, workflow promotion, stored workflow filtering, update/clone/remove, validation, and applicability explanation behavior.
- 2026-04-26: Extracted the `agentos auth` command group from `main.cpp` into `src/cli/auth_commands.*`, preserving provider listing, credential-store status, status/login/refresh/probe/logout/default-profile, OAuth PKCE start/callback/listen/complete, and token request helpers.
- 2026-04-26: Started decomposing `plugin_host.cpp` by extracting shared PluginSpec support validation / PluginSpec-to-CliSpec conversion into `plugin_spec_utils.*` and moving `PluginSkillInvoker` into `plugin_skill_invoker.cpp`.
- 2026-04-26: Continued `plugin_host.cpp` decomposition by extracting plugin workspace sandbox path-argument containment into `plugin_sandbox.*`.
- 2026-04-26: Continued `plugin_host.cpp` decomposition by extracting shared plugin JSON helpers, manifest loader, output schema validator, and JSON-RPC request/response helpers into dedicated plugin modules.
- 2026-04-26: Finished `plugin_host.cpp` decomposition by extracting persistent JSON-RPC session lifecycle, health checks, and PluginHost runtime execution into dedicated plugin modules.
- 2026-04-24: Extended `storage migrate` to move legacy root-level storage files into `runtime/`, normalize known legacy memory/workflow/scheduler schemas, and report normalized targets.
- 2026-04-24: Extended `storage compact target=audit` to rebuild task lifecycle audit entries from `runtime/memory` even when `audit.log` is missing, preserve non-task audit events, reinsert task-scoped preserved events such as `policy`, preserve orphan task-scoped preserved events for tasks absent from memory logs, reuse prior lifecycle timestamps, merge task bundles plus timestamped preserved task events into stable chronological order, and place untimed global preserved events after timed chunks.
- 2026-04-25: Extended `storage compact target=audit` to rebuild `scheduler_run` audit events from `runtime/scheduler/runs.tsv`, replacing stale scheduler-run audit rows while preserving unrelated global audit events and 64-bit epoch-millisecond fields.
- 2026-04-24: Added Windows Job Object resource controls for CLI skills through `CliSpec.max_processes` / `memory_limit_bytes`, plus smoke coverage for enforced process-count limits.
- 2026-04-24: Added GitHub Actions CI for Windows + Ubuntu configure/build/test and documented matching local CI commands in `README.md`.
- 2026-04-24: Added negative-path smoke coverage for remote tasks without TrustPolicy, blocked remote peers, invalid scheduler `missed_run_policy` normalization, and auth missing-session / unregistered-provider failures.
- 2026-04-24: Added `agentos_cli_integration_tests` to exercise real CLI command groups (`agents`, `memory`, `storage`, `trust`, `schedule`, `subagents`, `auth`) through the built executable.
- 2026-04-24: Started splitting `core_smoke_tests.cpp` by adding `agentos_storage_tests` for storage/atomic-file/compaction coverage while keeping the original smoke suite intact.
- 2026-04-24: Continued the test split with `agentos_auth_tests`, moving auth/session/provider fixture coverage into a dedicated test target while keeping the original smoke suite intact.
- 2026-04-24: Continued the test split with `agentos_policy_trust_tests`, moving policy/permission/trust/pairing coverage into a dedicated test target while keeping the original smoke suite intact.
- 2026-04-24: Continued the test split with `agentos_scheduler_tests`, moving scheduler due-run/retry/missed-run coverage into a dedicated test target while keeping the original smoke suite intact.
- 2026-04-24: Continued the test split with `agentos_workflow_router_tests`, moving workflow execution, router preference, lesson-aware routing, execution-cache, and workflow-store persistence coverage into a dedicated test target while keeping the original smoke suite intact.
- 2026-04-24: De-duplicated `agentos_smoke_tests` by removing storage/auth/policy-trust/scheduler/workflow-router invocations now covered by dedicated test targets, leaving smoke focused on file-skill, subagent, workspace-session, CLI, and plugin paths.
- 2026-04-24: Continued the test split with `agentos_subagent_session_tests`, moving subagent orchestration and workspace-session coverage into a dedicated test target, and narrowed `agentos_smoke_tests` to file-skill, CLI, and plugin paths.
- 2026-04-24: Continued the test split with `agentos_cli_plugin_tests`, moving CLI host, external CLI spec, plugin host, and jq fixture coverage into a dedicated test target, and narrowed `agentos_smoke_tests` to file-skill and policy-smoke coverage.
- 2026-04-24: Completed the smoke-test split by replacing `agentos_smoke_tests` with `agentos_file_skill_policy_tests`, deleting the old monolithic `core_smoke_tests.cpp`, and keeping coverage in dedicated module-level targets only.
- 2026-04-24: Added shared `tests/test_command_fixtures.hpp` helpers for cross-platform env overrides, PATH injection, and CLI fixture scripts, and reused them across auth and CLI/plugin tests.
- 2026-04-24: Replaced the POSIX CLI `popen` path with a `fork`/`execve` runner that preserves cwd/env/output/timeout behavior and applies `CliSpec.memory_limit_bytes` / `max_processes` through `setrlimit` where available.
- 2026-04-24: Added `CliSpec.cpu_time_limit_seconds` and `file_descriptor_limit`; CPU limits are enforced by Windows Job Objects and POSIX `RLIMIT_CPU`, and file descriptor limits are enforced on POSIX through `RLIMIT_NOFILE`.
- 2026-04-24: Extended PluginSpec parsing and PluginHost execution to pass memory/process/CPU/file-descriptor resource limits through to the underlying CliHost.
- 2026-04-24: Added WorkflowStore `input_equals` applicability conditions, router matching, CLI promotion support, persistence, and workflow/router regression coverage.
- 2026-04-24: Added WorkflowStore numeric range applicability conditions through `input_number_gte` / `input_number_lte`, including router matching, CLI promotion support, persistence, and regression coverage.
- 2026-04-24: Added WorkflowStore boolean input applicability conditions through `input_bool`, including router matching, CLI promotion support, persistence, docs, and regression coverage.
- 2026-04-25: Added WorkflowStore regex input applicability conditions through `input_regex`, including router matching, CLI promotion support, persistence, legacy compaction schema update, docs, and regression coverage.
- 2026-04-25: Added WorkflowStore composite OR applicability groups through `input_any`, including router matching, CLI promotion support, persistence, legacy compaction schema update, docs, and regression coverage.
- 2026-04-25: Added WorkflowStore nested boolean applicability expressions through `input_expr`, including `&&`, `||`, `!`, parentheses, router matching, CLI promotion support, persistence, legacy compaction schema update, docs, and regression coverage.
- 2026-04-25: Added shared workflow definition validation, wired it into `memory promote-workflow`, exposed `memory validate-workflows`, and covered invalid condition diagnostics in unit and CLI integration tests.
- 2026-04-25: Added workflow applicability explanation through `memory explain-workflow`, backed by shared evaluation helpers and CLI integration coverage for matching and non-matching inputs.
- 2026-04-25: Added `memory show-workflow` to inspect a single stored workflow definition, including steps, stats, enabled state, and applicability conditions.
- 2026-04-25: Added `memory set-workflow-enabled` and `memory remove-workflow` so stored workflow definitions can be toggled or deleted from the CLI.
- 2026-04-25: Added `memory update-workflow` for validated in-place edits to stored workflow names, trigger, steps, enabled state, and applicability conditions, including clearing list-valued conditions.
- 2026-04-25: Added `memory clone-workflow` to copy stored workflow definitions under a new name for further editing.
- 2026-04-25: Added filters to `memory stored-workflows` for enabled state, trigger task type, source, and name substring.
- 2026-04-26: Refactored `SecureTokenStore` around an `ISecureTokenBackend` abstraction and added macOS Keychain (Security framework) and Linux Secret Service (libsecret, optional CMake dependency) backends with an in-memory test backend so unit tests never touch the real keychain. `auth credential-store` now reports `windows-credential-manager` / `macos-keychain` / `linux-secret-service` / `env-ref-only` per platform.
- 2026-04-26: Broadened OAuth provider discovery: added `origin` (builtin/config/stub/none) and `note` metadata to `OAuthProviderDefaults`, registered stub entries for `openai` / `anthropic` / `qwen`, and extended `auth oauth-config-validate` with `--all` to enumerate every registered provider in one shot.
- 2026-04-26: Authored `docs/ADR-JSON-001.md` (Proposed) recommending nlohmann/json adoption with a phased migration plan. No code or build-system changes yet.
- 2026-04-26: Wired `src/scheduler/cron.{hpp,cpp}` and `src/scheduler/timezone.{hpp,cpp}` into `Scheduler::save` / `run_due` / cron validation, replacing the inline UTC-only cron parser. Added `ScheduledTask.timezone_name` (TSV column 28, backward-compatible legacy load), `Scheduler::IsTimezoneValid`, and a tz-aware `NextCronRunEpochMs` overload. `agentos schedule add` now accepts `timezone=America/New_York` / `tz=UTC+08:00` and `schedule list` surfaces the column. Added `TestSchedulerCronTimezoneAwareness` and `TestSchedulerCronTimezoneRoundTrip` covering UTC default, fixed-offset zones, curated IANA zones, and TSV round-trip.
- 2026-04-26: Guarded `<windows.h>` includes in `anthropic_agent.cpp` / `codex_cli_agent.cpp` / `qwen_agent.cpp` with `#define NOMINMAX` so MSVC `/permissive-` no longer surfaces the Windows `min`/`max` macros and rejects `std::max(...)` calls in agent V2 / curl secret-staging code paths; restored a green local build across all 11 CTest targets.
- 2026-04-26: Migrated `GeminiAgent` onto `IAgentAdapterV2`. `invoke()` now wraps the existing REST/CLI run path with a `CancellationToken` pre-check and emits `SessionInit` (carrying the normalized model name from `invocation.constraints`), `Status`, zero-cost `Usage`, and `Final` events; `cancel(task_id)` is a legacy no-op stub matching the rest of the V2-migrated adapters. Added `TestGeminiAgentV2InvokeEmitsLifecycleEvents` and `TestGeminiAgentV2InvokeCancelsBeforeDispatch` to `agentos_agent_provider_tests`.
- 2026-04-26: Wired `SubagentManager::run_one` to prefer the V2 `invoke()` path on adapters that implement `IAgentAdapterV2`. Build a structured `AgentInvocation` (task_type, parent_task_id, agent, role, original_objective, subtask_objective in `context`) and dispatch through `invoke()`; legacy adapters fall back to `run_task()`. `step.estimated_cost` now sources from `AgentResult.usage.cost_usd` when populated, otherwise from legacy `estimated_cost`. Added `TestSubagentManagerPrefersV2InvokeWhenAvailable` (with a dual-path fake adapter) to `agentos_subagent_session_tests`, asserting V2 is called once, legacy zero times, the V2 measured cost wins over the legacy estimate, and the structured context survives the TaskRequest→AgentInvocation translation.
- 2026-04-26: Plumbed an optional `std::shared_ptr<CancellationToken>` through `SubagentManager::run` and `run_one`. Sequential mode short-circuits subsequent dispatches once the token is tripped; parallel mode propagates the token into each `std::async` task and the run_one prologue records a `Cancelled` step instead of dispatching. The token is also forwarded into `AgentInvocation::cancel`, so V2 adapters that honor it can interrupt mid-call. Added `TestSubagentManagerCancellationShortCircuitsSequential` and `TestSubagentManagerCancellationShortCircuitsParallel` covering pre-tripped tokens for both execution modes.
- 2026-04-26: Added `src/utils/signal_cancellation.{hpp,cpp}` with `InstallSignalCancellation()` — an idempotent process-wide installer for a Ctrl-C / SIGINT (and SIGTERM on POSIX) handler that trips a shared `CancellationToken`. First signal is swallowed and prints a single hint to stderr; second signal restores the OS default disposition (Windows: returns FALSE from the console-control handler; POSIX: re-raises after `sigaction(SIG_DFL)`) so users can always force-kill. Wired it into `agentos run` and `agentos subagents run` so live CLI dispatch is now cancellable. Extended `AgentLoop::run` with the optional cancel token, added a pre-routing fast-fail check, and converted `run_agent_task` to prefer `IAgentAdapterV2::invoke()` when available (with cancel token forwarded into `AgentInvocation::cancel` and `step.estimated_cost` sourced from `AgentResult.usage.cost_usd` when populated). Added `TestAgentLoopRoutesThroughV2InvokeAndForwardsUsageCost`, `TestAgentLoopHonorsCancellationBeforeAgentDispatch`, and `TestSignalCancellationIsIdempotent` to `agentos_subagent_session_tests`.
- 2026-04-26: Added `agentos auth login-interactive [provider=<id>]`, a stdin-driven prompt that surfaces per-provider OAuth-defaults `origin`/`note` metadata, picks a mode from the provider's supported set, and re-enters the existing `auth login` handler so the AuthManager/SessionStore path is not duplicated. Prompt logic lives in `src/cli/auth_interactive.{hpp,cpp}` and is unit-tested via `std::stringstream`; CLI integration coverage exercises the full binary with stdin redirected through a temp file.
- 2026-04-26: Added a dedicated `agentos_cancellation_tests` target (12th CTest target). Direct unit coverage for `CancellationToken` (idempotent cancel, `wait_for_cancel` timeout vs. immediate trip, multi-thread `notify_all` releasing all waiters) and `InstallSignalCancellation` (idempotent install returning the same shared token across calls; observability from worker threads). `tests/cancellation_tests.cpp`; `CLAUDE.md` test count and roster bumped to 12.
- 2026-04-26: Added `docs/V2_ADAPTER_INTERFACE.md` (~3.3k words) — full reference for `IAgentAdapterV2`: type signatures quoted from `src/core/models.hpp`, canonical event sequence (SessionInit → Status → TextDelta* → ToolUseStart/Result* → Usage → Final), cancellation contract (token observability, `Cancelled` error_code, two-signal force-kill semantics), V1→V2 migration recipe walked through `LocalPlanningAgent`, routing semantics (`SubagentManager` / `AgentLoop` `dynamic_cast` + `usage.cost_usd` flow into `step.estimated_cost`), and known gaps. Added a `4.7 V2 adapter interface` summary subsection in `docs/AGENT_SYSTEM.md` linking to the full reference.
- 2026-04-26: Multi-agent parallel development round: docs slice (V2 adapter reference), tests slice (`agentos_cancellation_tests`), and auth UX slice (`auth login-interactive`) shipped as three file-disjoint tracks running concurrently in isolated worktrees. All 12 CTest targets pass.
- 2026-04-26: Added `docs/CANCELLATION.md` (~2.9k words / 582 lines) — focused infrastructure reference for cooperative cancellation: `CancellationToken` and `InstallSignalCancellation()` declarations quoted verbatim, propagation diagram from signal → CLI driver → AgentLoop → SubagentManager → AgentInvocation → V2 adapter checkpoints, two-signal force-kill semantics with platform differences (Windows console-control handler returning FALSE vs POSIX `sigaction(SIG_DFL)` + `raise`), V2 adapter contract with Codex / Anthropic / Qwen streaming examples and Gemini / LocalPlanning sync-wrap pattern, testing patterns referencing `tests/cancellation_tests.cpp` and `tests/subagent_session_tests.cpp`, and known gaps (legacy adapters, skill-routed runs, scheduler daemon mode).
- 2026-04-26: Refreshed `completion_review.md` (403→533 lines) with twelve inline `> 2026-04-26 update:` annotations across §1/§2/§3/§4/§5/§6/§10 and a new §11 "多代理并行开发轮次复核（2026-04-26）" covering V2 adapter migration end-to-end, cancellation infrastructure plumbing, two parallel multi-agent rounds, refreshed test count (11→12), plan.md item closure status, ongoing doc/code drift items, a refreshed module-completion radar, and updated score recommendations.
- 2026-04-26: Added `agentos diagnostics [format=text|json]` — new top-level read-only CLI subcommand consolidating platform / build / auth / skills / agents / plugins / scheduler / storage / trust state into one snapshot. Implementation in `src/cli/diagnostics_commands.{cpp,hpp}` follows the explicit-references signature convention (matches `RunStorageCommand`); each section is collected in its own try/catch with errors reflected as `<section>_error` JSON keys / `ERROR <message>` text lines, never aborting the snapshot. Reuses existing helpers (`MakeJsonObject` / `QuoteJson` / `NumberAsJson` / `LoadCliSpecsWithDiagnostics` / `LoadPluginSpecsWithDiagnostics` / `LoadPluginHostOptions` / `Scheduler::due` / `SecureTokenStore::status` / `CurrentStoragePolicy`); no mutating call sites. Added `TestDiagnosticsCommand` to `agentos_cli_integration_tests` covering text + JSON formats. README `当前实现状态` updated to enumerate the V2 adapter, cooperative cancellation, login-interactive, and 12 CTest targets.
- 2026-04-26: Multi-agent parallel development round 2: completion_review refresh, `docs/CANCELLATION.md`, and `agentos diagnostics` CLI shipped as three file-disjoint tracks. All 12 CTest targets pass under serial execution; parallel ctest occasionally surfaces a pre-existing flake in `agentos_cli_plugin_tests` (subprocess fixture race on Windows that is independent of round 2 work — direct binary execution and `ctest -j 1` are 100% clean).
- 2026-04-26: Closed the open `docs/CANCELLATION.md` "Known Gaps" item for scheduler daemon mode. `Scheduler::run_due` now takes an optional `std::shared_ptr<CancellationToken>` (default null preserves callers); a tripped token short-circuits dispatch between scheduled tasks and is forwarded into `AgentLoop::run` so individual task runs honor cancellation. `RunSchedulerLoop` (used by both `agentos schedule tick` and `agentos schedule daemon`) now installs the process-wide signal handler, replaces `std::this_thread::sleep_for` with `cancel->wait_for_cancel(interval_ms)` so Ctrl-C interrupts the inter-iteration wait promptly, and prints a `<label> cancelled iteration=<n>` line before exiting. Added `TestSchedulerSkipsDispatchWhenCancelled` to `agentos_scheduler_tests` proving zero tasks dispatch when the token is pre-tripped.
- 2026-04-26: Diagnosed and fixed the real `agentos_cli_plugin_tests` flake. Bisecting via instrumented step markers narrowed the segfault to `TestPluginSpecLoaderAndInvoker`'s persistent JSON-RPC powershell session. Root cause: `CreateProcessA` calls in both `src/hosts/cli/cli_host.cpp::RunProcessWindows` and `src/hosts/plugin/plugin_persistent_session.hpp::start` passed `bInheritHandles=TRUE` with a plain `STARTUPINFOA`, which inherits every inheritable parent-side handle into the child — including CTest's stdout/stderr pipe handles when these code paths ran inside the test binary under CTest. A long-running powershell child therefore held CTest's pipe alive past the test process's exit, racing the harness's pipe teardown and intermittently faulting. Fix: switched both call sites to `STARTUPINFOEXA` + `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` whitelisting exactly `stdin_read` / `stdout_write` / `stderr_write`, plus `EXTENDED_STARTUPINFO_PRESENT` in the creation flags. Verified with 15 consecutive `ctest -R agentos_cli_plugin_tests` runs and 5 consecutive full `ctest` runs — 100% pass. Removed the `RUN_SERIAL` workaround properties from `agentos_cli_plugin_tests` and `agentos_cli_integration_tests`.
- 2026-04-27: Refreshed stale "11 tests" / "11/11" references in `plan.md` (Completion Snapshot, Review Findings, validation note) and `completion_review.md` §0 score table to "12 tests"; historical progress-log entries were left intact. Verified 12/12 CTest targets pass under `ctest -j 1`.
- 2026-04-27: Promoted `docs/ADR-JSON-001.md` from **Proposed** to **Accepted (Phase 1 partial)** and added a §9 "Implementation Progress" section enumerating what landed (FetchContent build wire-in; nlohmann migrations in `json_utils.cpp::NumberAsJson`, `subagent_manager.cpp::ExtractPlanActions`, `provider_adapters.cpp` CLI-probe parsing, the four V2 streaming agent adapters, and `workflow_run_skill.cpp`), divergences from §6 ordering (FetchContent instead of vendoring; subagent_manager / provider_adapters / V2 adapters first instead of Plugin Host first), and what remains hand-rolled (audit_logger, plugin schema validator, four builtin skills, OAuth token-exchange JSON, `cli_spec_loader` schema strings).
- 2026-04-27: Refreshed `docs/AUTH_DESIGN.md` §11 and `docs/AUTH_PRD.md` §9 to enumerate `agentos auth login-interactive`, the `src/utils/curl_secret.{hpp,cpp}` short-lived temp-file routing for OAuth body / bearer tokens (so secrets never reach argv), and the OAuth callback path / loopback-Host hardening in `src/auth/oauth_pkce.cpp`. Both docs already covered the Windows / macOS / Linux credential-store backends — annotations now match §11 of the completion review.
- 2026-04-27: Polished `agentos auth login-interactive` UX. Unknown `provider=<id>` arguments now print the list of registered providers on stderr before exiting non-zero, so the user can recover without grep'ing source. Choosing `browser_oauth` now produces a copy-paste-ready `agentos auth oauth-login <provider> client_id=<CLIENT_ID> redirect_uri=http://127.0.0.1:48177/callback port=48177 open_browser=true profile=default set_default=true` template in the redirect message, and annotates the message when the provider's OAuth defaults `origin` is `stub` / `config` so the user knows whether builtin defaults exist or `runtime/auth_oauth_providers.tsv` overrides are required first. All 12 CTest targets stay green; the existing `TestInteractiveLoginBrowserOAuthRedirects` assertion (message must contain `auth oauth-login`) still passes.
- 2026-04-27: ADR-JSON-001 phase 2 step. Migrated `src/skills/builtin/workflow_run_skill.cpp` (both built-in `write_patch_read` and stored-workflow paths) to build the result object via `nlohmann::json` and embed each step's `json_output` through a new `EmbedStepOutput()` helper that parses the child output as a sub-object when valid JSON and falls back to a JSON string otherwise. Closes the ADR §1.2 double-escaping bug — `final_output` is now `{"path":...,"content":...}` instead of `"{\"path\":...,\"content\":...}"`. No tests asserted on the prior double-escaped form; all 12 CTest targets pass.
- 2026-04-27: ADR-JSON-001 phase 2 batch. Migrated the four remaining builtin skills (`file_read_skill.cpp`, `file_write_skill.cpp`, `file_patch_skill.cpp`, `http_fetch_skill.cpp`), the CLI skill invoker (`src/hosts/cli/cli_skill_invoker.cpp` — the shared result builder for every CLI skill including `rg_search` / `git_status` / `git_diff` / `curl_fetch` / `jq_transform` plus every external `runtime/cli_specs/*.tsv` skill), and the local planning agent (`src/hosts/agents/local_planning_agent.cpp` — the `content/agent/provider/task_type/objective/plan_steps[]` structured plan output). Each site now builds via `nlohmann::json` and emits a single `dump()` so escaping happens once, deterministically. ADR §9.1 and §9.3 updated to reflect that all five builtin skills, the CLI invoker, and the local planner are now migrated. All 12 CTest targets pass.
- 2026-04-27: ADR-JSON-001 phase 2 — `audit_logger.cpp` migrated. All seven `record_*` emission sites (`task_start`, `route`, `policy`, `step`, `task_end`, `config_diagnostic`, `trust`) and all four compaction-recovery emission sites (rebuilt `task_start` / `route` / `step` / `task_end` plus `scheduler_run`) now build `nlohmann::ordered_json` and `dump()` it. `ordered_json` was chosen so the on-disk JSONL stays byte-identical to the previous `MakeJsonObject` output (insertion order preserved across `ts → event → task_id → ...`) — important because the test fixture in `tests/storage_tests.cpp` greps for `"event":"policy","task_id":"task-1"` adjacency. All 12 CTest targets pass.
- 2026-04-27: ADR-JSON-001 phase 2 — OAuth token-exchange parsing migrated. `src/auth/oauth_pkce.cpp::JsonStringField` and `JsonIntField` now parse via `nlohmann::json::parse` inside a `try/catch (const nlohmann::json::exception&)`; the legacy `SkipWhitespace`/`ParseJsonStringAt`/`FindTopLevelJsonValue` helpers were later deleted. Whitespace, key order, unicode escapes, and nested objects in `access_token` / `refresh_token` / `expires_in` / `error` / `error_description` round-trip correctly. All 12 CTest targets pass.
- 2026-04-27: ADR-JSON-001 phase 2 — schema columns parse-validated at load time. `src/hosts/cli/cli_spec_loader.cpp` and `src/hosts/plugin/plugin_manifest_loader.cpp` replaced the legacy `IsLikelyJsonObjectString` bracket-shape check with a new local `IsParseableJsonObject` that calls `nlohmann::json::parse` and asserts `is_object()`. Manifest column malformations the legacy check would have accepted (unterminated strings, mismatched braces inside content, invalid escape sequences) now produce a skipped-spec diagnostic at startup. Phase 2 of ADR-JSON-001 §6 is now partially closed; phase 3 (Auth) and phase 4 (Audit + skills) are fully closed. All 12 CTest targets pass.
- 2026-04-27: ADR-JSON-001 phase 5 cleanup slice — `src/cli/diagnostics_commands.cpp` no longer uses `json_utils` for `agentos diagnostics format=json`. The renderer now builds a typed `nlohmann::ordered_json` snapshot with nested provider/agent arrays and dumps it once; text diagnostics are unchanged. Verified with MSVC `cmake --build build` and `ctest --test-dir build -R agentos_cli_integration_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 cleanup slice — `src/core/orchestration/agent_result_normalizer.cpp` now builds `agent_result.v1` envelopes through `nlohmann::ordered_json` instead of `MakeJsonObject` / `QuoteJson`, including typed `artifacts`, `metrics`, `provider_metadata`, and parsed-or-string `raw_output`. Verified with MSVC `cmake --build build`, `ctest --test-dir build -R "agentos_subagent_session_tests|agentos_agent_provider_tests" --output-on-failure`, and the full 12-target CTest suite.
- 2026-04-27: ADR-JSON-001 plugin slice — `src/hosts/plugin/plugin_json_rpc.cpp::JsonRpcRequestForPlugin` now builds JSON-RPC 2.0 request envelopes through `nlohmann::ordered_json`, removing `json_utils` from request rendering while leaving response validation on the existing plugin JSON helper path. Verified with MSVC `cmake --build build`, `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`, and the full 12-target CTest suite.
- 2026-04-27: Stabilized the persistent plugin restart path uncovered while verifying the JSON-RPC request-builder slice. `PluginHost::run` no longer erases `sessions_[session_key]` while holding a reference to that map slot; it resets the stale `unique_ptr` before starting a replacement session, avoiding the intermittent Windows access violation exercised by the persistent crash fixture. Added direct `JsonRpcRequestForPlugin` coverage for empty params and escaped values in `agentos_cli_plugin_tests`. Verified with MSVC `cmake --build build`, 10 consecutive `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure` runs, and the full 12-target CTest suite.
- 2026-04-27: ADR-JSON-001 plugin slice — `src/hosts/plugin/plugin_json_rpc.cpp::JsonRpcOutputError` and `JsonRpcResultObject` now parse JSON-RPC 2.0 responses with `nlohmann::ordered_json` instead of the plugin JSON helper parser. Public error strings are preserved (`must be a JSON object`, missing `jsonrpc`, missing `id`, error response, non-object `result`), and result extraction still returns a JSON object string for schema validation / `plugin_output` projection. Added direct response-validation coverage in `agentos_cli_plugin_tests`; verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 plugin schema slice — `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now parses `output_schema_json` and plugin output through `nlohmann::ordered_json` and evaluates the existing supported keyword subset (`required`, `properties.*.type`, string constraints, numeric bounds / `multipleOf`, object/property constraints, dependencies, conditionals, and allOf/anyOf/oneOf`) against typed JSON values instead of raw string offsets. Removed the unused plugin JSON helper include from `plugin_execution.cpp`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 plugin manifest slice — `src/hosts/plugin/plugin_manifest_loader.cpp` now parses `*.json` plugin manifests with `nlohmann::ordered_json` for scalar fields, string arrays, bools, resource limits, and object/string schema fields; object-valued schemas are preserved via `dump()`. Strict JSON parsing now rejects non-standard trailing commas, so JSON plugin fixtures in `tests/cli_plugin_tests.cpp` and `tests/cli_integration_tests.cpp` were tightened to valid JSON. With JSON-RPC and schema validation already migrated, `src/hosts/plugin/plugin_json_utils.{hpp,cpp}` had no live callers and was deleted from the build. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_cli_integration_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_cli_integration_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 plugin cleanup slice — `src/hosts/plugin/plugin_skill_invoker.cpp` now builds plugin skill result envelopes with `nlohmann::ordered_json` and projects `json-rpc-v0` results through the shared `JsonRpcResultObject()` helper instead of scanning stdout for `"result"` and brace-counting manually. `src/hosts/plugin/` no longer includes `utils/json_utils.hpp`. Added parsed-envelope assertions for stdio-json and JSON-RPC plugin skill output in `agentos_cli_plugin_tests`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 cleanup slice — `src/core/loop/agent_loop.cpp` now builds legacy `AgentTask.context_json` and model-only `constraints_json` with `nlohmann::ordered_json`, removing the core loop's dependency on `utils/json_utils.hpp` while preserving the compact JSON shape consumed by legacy adapters. Verified with MSVC `cmake --build build --target agentos_workflow_router_tests agentos_subagent_session_tests` and `ctest --test-dir build -R "agentos_workflow_router_tests|agentos_subagent_session_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 cleanup slice — `src/core/orchestration/subagent_manager.cpp` now builds aggregate subagent orchestration output, embedded `agent_outputs[]` entries, optional `decomposition_agent`, decomposition-agent context, and per-agent legacy `AgentTask.context_json` through `nlohmann::ordered_json`. This removes `utils/json_utils.hpp` from `SubagentManager` while preserving parsed `normalized` / `output` sub-objects where child agents returned object-shaped JSON. Verified with MSVC `cmake --build build --target agentos_subagent_session_tests` and `ctest --test-dir build -R agentos_subagent_session_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 provider cleanup slice — `src/hosts/agents/gemini_agent.cpp` no longer includes `utils/json_utils.hpp`. Gemini REST / CLI-session legacy provider output envelopes and the `generateContent` request body now build through `nlohmann::ordered_json`, and REST response text extraction parses `candidates[].content.parts[].text` with `nlohmann::json` instead of manual string scanning. Verified with MSVC `cmake --build build --target agentos_agent_provider_tests` and `ctest --test-dir build -R agentos_agent_provider_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 provider cleanup slice — `src/hosts/agents/anthropic_agent.cpp` no longer includes `utils/json_utils.hpp`. Anthropic REST / Claude CLI / streaming legacy provider output envelopes and the Messages request body now build through `nlohmann::ordered_json`, and REST response text extraction parses `content[].text` with `nlohmann::json` instead of manual string scanning. Verified with MSVC `cmake --build build --target agentos_agent_provider_tests` and `ctest --test-dir build -R agentos_agent_provider_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 provider cleanup slice — `src/hosts/agents/codex_cli_agent.cpp` no longer includes `utils/json_utils.hpp`. Codex CLI legacy and V2 NDJSON streaming provider output envelopes plus artifact metadata now build through `nlohmann::ordered_json`. Verified with MSVC `cmake --build build --target agentos_agent_provider_tests` and `ctest --test-dir build -R agentos_agent_provider_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 provider cleanup slice — `src/hosts/agents/qwen_agent.cpp` no longer includes `utils/json_utils.hpp`. Qwen REST / V2 streaming provider output envelopes, Chat Completions request bodies, REST response content extraction, and V2 constraint projection now build/parse through `nlohmann::ordered_json` / `nlohmann::json`. Verified with MSVC `cmake --build build --target agentos_agent_provider_tests` and `ctest --test-dir build -R agentos_agent_provider_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 phase 5 retirement slice — `tests/workflow_router_tests.cpp` and `tests/subagent_session_tests.cpp` no longer use `MakeJsonObject` / `QuoteJson`; test fixture JSON is now built through `nlohmann::ordered_json`. Removed stale include-only references from `src/auth/provider_adapters.cpp` and `src/skills/builtin/workflow_run_skill.cpp`, deleted `src/utils/json_utils.{hpp,cpp}`, and removed `src/utils/json_utils.cpp` from `CMakeLists.txt`. `rg` now shows no `json_utils` / helper references under `src`, `tests`, or `CMakeLists.txt`. Verified with MSVC `cmake --build build --target agentos_workflow_router_tests agentos_subagent_session_tests` and `ctest --test-dir build -R "agentos_workflow_router_tests|agentos_subagent_session_tests" --output-on-failure`.
- 2026-04-27: Hygiene pass — removed trailing whitespace from the dirty plugin manifest/session, main CLI, and plugin test files that were causing `git diff --check` to fail. Verified `git diff --check` is clean.
- 2026-04-27: ADR-JSON-001 schema companion slice — `src/core/schema/schema_validator.{hpp,cpp}` now exposes `IsParseableJsonObjectSchema()`, and `src/hosts/cli/cli_spec_loader.cpp` plus `src/hosts/plugin/plugin_manifest_loader.cpp` use it for parse-time `input_schema_json` / `output_schema_json` object validation instead of duplicating local helpers. Verified with MSVC `cmake --build build --target agentos_spec_parsing_tests agentos_cli_plugin_tests` and `ctest --test-dir build -R "agentos_spec_parsing_tests|agentos_cli_plugin_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved the runtime JSON-object `required` keyword check into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectRequiredFieldValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates missing required output-field detection to the shared schema module while preserving the existing public error string. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object `properties.*.type` output validation into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectPropertyTypeValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates type mismatch detection to the shared schema module while preserving existing public error strings such as `plugin output field has invalid type: message:string`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object `properties.*` string output constraints (`const`, `enum`, `minLength`, `maxLength`, `pattern`) into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectStringConstraintValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates those checks before its remaining numeric/object/branch keyword checks while preserving existing public error strings such as `plugin output field has invalid constraint: message:pattern`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object `properties.*` numeric output constraints (`minimum`, `maximum`, `exclusiveMinimum`, `exclusiveMaximum`, `multipleOf`) into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectNumericConstraintValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates those checks before its remaining object/branch keyword checks while preserving existing public error strings such as `plugin output field has invalid constraint: score:multipleOf`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object output shape constraints (`minProperties`, `maxProperties`, `propertyNames`) and `additionalProperties:false` validation into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectShapeConstraintValidationError()` and `JsonObjectAdditionalPropertiesValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` delegates shape checks at the original early point and additional-property checks at the original final point, preserving public error strings such as `plugin output failed minProperties` and `plugin output field has invalid constraint: extra:additionalProperties`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object output dependency validation (`dependentRequired` and legacy array-valued `dependencies`) into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectDependentRequiredValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates dependency checks at the original point before `not.required` / conditional / branch checks while preserving public error strings such as `plugin output field has invalid constraint: token:dependentRequired:signature`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object output `not.required` and basic `if` / `then` / `else` required-branch validation into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectNotRequiredValidationError()` and `JsonObjectIfThenElseRequiredValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates those checks before the remaining allOf/anyOf/oneOf branch validation while preserving public error strings such as `plugin output failed not.required` and `plugin output failed then required field: token`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion slice — moved JSON-object output `allOf` / `anyOf` / `oneOf` required-branch validation into `src/core/schema/schema_validator.{hpp,cpp}` as `JsonObjectRequiredBranchValidationError()`. `src/hosts/plugin/plugin_schema_validator.cpp::PluginOutputSchemaError` now delegates all supported output schema keyword checks to shared `core/schema` helpers while preserving public error strings such as `plugin output failed anyOf required branches` and `plugin output failed oneOf required branches`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests` and `ctest --test-dir build -R agentos_cli_plugin_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` now extracts top-level `input_schema_json.required` through a typed `nlohmann::ordered_json` parse helper instead of the legacy string scanner, preserving `SchemaValidationFailed` missing-field behavior while starting the remaining skill-input consolidation path. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input object-shape checks (`propertyNames`, `minProperties`, and `maxProperties`) in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputShapeConstraintFailures()`, preserving `SchemaValidationFailed` tokens such as `schema:propertyNames:pattern:BadName`, `schema:minProperties`, and `schema:maxProperties`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input dependency checks (`dependentRequired` and legacy array-valued `dependencies`) in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputDependentRequiredConstraintFailures()`, preserving `SchemaValidationFailed` tokens such as `token:dependentRequired:signature` and `receipt:dependentRequired:mode`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input `not.required` and basic `if` / `then` / `else` required-branch checks in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputNotRequiredConstraintFailures()` and `JsonObjectInputIfThenElseRequiredConstraintFailures()`, preserving `SchemaValidationFailed` tokens such as `schema:not:required`, `schema:then:required:token`, and `schema:else:required:anonymous_reason`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input `allOf` / `anyOf` / `oneOf` required-branch checks in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputRequiredBranchConstraintFailures()`, preserving `SchemaValidationFailed` tokens `schema:anyOf:required`, `schema:oneOf:required`, and `schema:allOf:required`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input `additionalProperties:false` checks in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputAdditionalPropertiesConstraintFailures()`, preserving `SchemaValidationFailed` tokens such as `extra:additionalProperties`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input `properties.*.type` checks in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputPropertyTypeFailures()`, preserving string-encoded CLI argument semantics and `SchemaValidationFailed` tokens such as `count:integer`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input `properties.*` string constraint checks (`const`, `enum`, `minLength`, `maxLength`, `pattern`) in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputStringConstraintFailures()`, preserving `SchemaValidationFailed` tokens such as `label:enum`, `label:minLength`, `code:pattern`, and `mode:const`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 input schema companion slice — moved skill input `properties.*` numeric constraint checks (`minimum`, `maximum`, `exclusiveMinimum`, `exclusiveMaximum`, `multipleOf`) in `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` onto typed `nlohmann::ordered_json` parsing via `JsonObjectInputNumericConstraintFailures()`, preserving string-encoded CLI argument semantics and `SchemaValidationFailed` tokens such as `ratio:exclusiveMaximum`, `ratio:exclusiveMinimum`, and `ratio:multipleOf`. This retired the remaining legacy input `PropertySchema` scanner from `schema_validator.cpp`. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 OAuth cleanup slice — deleted the now-unused `[[maybe_unused]]` legacy token-response helpers (`SkipWhitespace`, `ParseJsonStringAt`, `FindTopLevelJsonValue`) from `src/auth/oauth_pkce.cpp` after token response parsing moved to `nlohmann::json`. Verified with MSVC `cmake --build build --target agentos_auth_tests` and `ctest --test-dir build -R agentos_auth_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — refactored `src/core/schema/schema_validator.cpp::ValidateRequiredInputFields` so runtime skill input validation parses `input_schema_json` once into `nlohmann::ordered_json` and passes the typed schema through the input helper chain, instead of reparsing the same schema once per keyword group. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests` and `ctest --test-dir build -R agentos_file_skill_policy_tests --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — centralized JSON-object parse guarding in `src/core/schema/schema_validator.cpp` by routing `IsParseableJsonObjectSchema()` and the public `JsonObject*ValidationError()` helpers through shared `ParseJsonObject()` / `ParseJsonObjectPair()` helpers, preserving plugin output schema error strings while removing repeated parse/exception boilerplate. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added shared typed `properties` traversal helpers in `src/core/schema/schema_validator.cpp` and reused them across skill input `properties.*` validation plus JSON-object output type/string/numeric/additionalProperties checks. This keeps existing validation order and error strings while reducing duplicated property-schema lookup loops. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — shared dependency and required-branch helpers in `src/core/schema/schema_validator.cpp` now drive both skill input validation and JSON-object output validation for `dependentRequired` / legacy `dependencies`, `not.required`, and `allOf` / `anyOf` / `oneOf` required branches. Existing `SchemaValidationFailed` tokens and plugin output error strings are preserved. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated `propertyNames` constraint evaluation in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input shape checks and JSON-object output shape checks. Input still collects `schema:propertyNames:*:<field>` tokens, and plugin output still returns the first `plugin output field has invalid constraint: <field>:propertyNames:*` error. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated object property-count evaluation in `src/core/schema/schema_validator.cpp` through a shared typed helper for `minProperties` / `maxProperties` used by both skill input and JSON-object output shape validation. Input still reports `schema:minProperties` / `schema:maxProperties`, and plugin output still reports `<subject> failed minProperties` / `<subject> failed maxProperties`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated basic `if` / `then` / `else` required-field traversal in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still reports `schema:then:required:<field>` / `schema:else:required:<field>`, and plugin output still reports `<subject> failed then required field: <field>` / `<subject> failed else required field: <field>`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated object-level `not.required` branch matching in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still reports `schema:not:required`, and plugin output still reports `<subject> failed not.required`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated `allOf` required-branch traversal in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still reports `schema:allOf:required`, and plugin output still reports `<subject> failed allOf required branches`; each caller keeps its existing validation order. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated `anyOf` / `oneOf` required-branch count validation in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still reports `schema:anyOf:required` / `schema:oneOf:required`, and plugin output still reports `<subject> failed anyOf required branches` / `<subject> failed oneOf required branches` while preserving each caller's validation order. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated top-level `required` missing-field traversal in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still accumulates all missing fields for `missing required input fields for <skill>: ...`, and plugin output still returns the first `<subject> missing required field: <field>` error. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated `properties.*.type` mismatch traversal in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input keeps string-encoded CLI argument type semantics (`integer` / `number` / `boolean` parsed from strings), and plugin output keeps native JSON value type semantics while preserving existing error strings. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated string length/pattern constraint evaluation in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still collects `field:minLength` / `field:maxLength` / `field:pattern` tokens, and plugin output still returns the first `<subject> field has invalid constraint: <field>:<constraint>` error while keeping existing `const` / `enum` ordering. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated numeric range / `multipleOf` constraint evaluation in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still parses string-encoded CLI arguments and reports `field:minimum` / `field:maximum` / `field:multipleOf` tokens, while plugin output still validates native JSON numbers and returns the first `<subject> field has invalid constraint: <field>:<constraint>` error. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests`, `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`, `git diff --check`, and the full `cmake --build build && ctest --test-dir build --output-on-failure` suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated `additionalProperties:false` field comparison in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still reports every extra CLI argument as `field:additionalProperties`, while plugin output still returns the first `<subject> field has invalid constraint: <field>:additionalProperties` error. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests`, `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`, `git diff --check`, and the full `cmake --build build && ctest --test-dir build --output-on-failure` suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated string `const` / `enum` value constraint evaluation in `src/core/schema/schema_validator.cpp` through a shared typed helper used by both skill input and JSON-object output validation. Input still reports `enum` before `const` when both fail, while plugin output still reports `string-value` for non-string JSON values and checks `const` before `enum`. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests`, `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`, `git diff --check`, and the full `cmake --build build && ctest --test-dir build --output-on-failure` suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated present property-value traversal in `src/core/schema/schema_validator.cpp` for skill input string/numeric constraints and JSON-object output string/numeric constraints. The callers still preserve prior type-failure skips, typed-output `type` guards, string-encoded input values, native JSON output values, and existing error strings. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests`, `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`, `git diff --check`, and the full `cmake --build build && ctest --test-dir build --output-on-failure` suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectSchemaValidationError()` as a parse-once JSON-object validation entry point and routed `PluginOutputSchemaError()` through it. The granular `JsonObject*ValidationError()` helpers remain available, but plugin output validation now parses `output_schema_json` and plugin output once while preserving the existing keyword order and public error strings. Verified with MSVC `cmake --build build --target agentos_cli_plugin_tests agentos_file_skill_policy_tests` and `ctest --test-dir build -R "agentos_cli_plugin_tests|agentos_file_skill_policy_tests" --output-on-failure`.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectInputConstraintFailures()` to centralize the skill-input constraint helper ordering after type validation. `ValidateRequiredInputFields()` still preserves required-field, type, and constraint error priority, while the input path now has a single collector for shape, dependency, branch, additionalProperties, string, and numeric constraint failures. Verified with MSVC `cmake --build build --target agentos_file_skill_policy_tests agentos_cli_plugin_tests` and `ctest --test-dir build -R "agentos_file_skill_policy_tests|agentos_cli_plugin_tests" --output-on-failure`.
- 2026-04-27: Workflow promotion regression slice — replaced the stale majority/no-majority TODO in `tests/workflow_router_tests.cpp` with direct `MemoryManager` fixtures that record synthetic task histories. Coverage now proves a 3-of-5 signature at exactly the 60% promotion threshold is promoted with the majority step sequence, while two 3-of-6 signatures at 50% do not promote. Verified with MSVC `cmake --build build --target agentos_workflow_router_tests`, `ctest --test-dir build -R agentos_workflow_router_tests --output-on-failure`, `git diff --check`, and the full `cmake --build build && ctest --test-dir build --output-on-failure` suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectValidationErrorForParsedPair()` so the granular public `JsonObject*ValidationError()` helpers share one schema/object parse guard and parsed-validator delegate path. This preserves all existing output schema error strings while removing the last repeated parse/delegate boilerplate in those helper entry points. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectSchemaConstraintValidationErrorForParsed()` so parse-once JSON-object output validation has the same required/type/constraint structure as skill input validation. The collector preserves the existing post-type keyword order and public plugin output schema errors. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectFieldConstraintError()` so JSON-object output constraint validators share the stable `<subject> field has invalid constraint: <field>:<constraint>` formatter across string, numeric, propertyNames, additionalProperties, and dependency checks. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectFailedConstraintError()` so JSON-object output validators share the stable `<subject> failed <constraint>` formatter across object-count, `not.required`, conditional required-field, and branch-combinator failures. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectRequiredFieldError()` and `JsonObjectFieldTypeError()` so JSON-object output validators also share the stable required-field and property-type error formatters. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectInputFieldConstraintFailure()`, `JsonObjectInputSchemaConstraintFailure()`, and `JsonObjectInputSchemaFieldConstraintFailure()` so skill-input schema validators share stable formatter helpers for `field:*` and `schema:*` failure tokens. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputFailures()` and `FirstJsonObjectValidationError()` visitor adapters so shared dependency, `additionalProperties:false`, and `not.required` validators can keep input's all-failures behavior and output's first-error behavior without duplicating callback plumbing. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — extended the visitor-adapter cleanup to property-count, conditional-required, and branch-combinator validators. Input still preserves the existing single `schema:allOf:required` token behavior, while output still returns the first formatted plugin schema error. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputPropertyConstraintFailures()` and `FirstJsonObjectPropertyConstraintError()` so present property-value string/numeric validators share one adapter shape while preserving input's all-token collection, output's first-error formatting, and the existing input/output string `const` / `enum` ordering difference. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — routed top-level required-field, `properties.*.type`, and `propertyNames` input/output wrappers through the same input-collection and output-first-error adapters, preserving existing missing-field, type, and property-name error text. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectSchemaValidationErrorForParsed()` and routed the public parse-once `JsonObjectSchemaValidationError()` entry point through `JsonObjectValidationErrorForParsedPair()`, so both aggregate and granular JSON-object output validators share the same parsed-pair delegate path while preserving required/type/constraint ordering. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputPropertyNameConstraintFailures()` and `FirstJsonObjectPropertyNameConstraintError()` so `propertyNames` validation shares field-list traversal as well as the low-level constraint visitor while preserving input all-token collection and output first-error behavior. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `JsonObjectInputRequiredFieldFailures()`, `FirstJsonObjectRequiredFieldError()`, and `FirstJsonObjectPropertyTypeError()` so required-field and JSON-object output type wrappers use named adapter helpers instead of carrying callback plumbing inline. Existing missing-field and type error ordering/text are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputPropertyCountFailures()` and `FirstJsonObjectPropertyCountError()` so `minProperties` / `maxProperties` input/output wrappers share named adapter helpers instead of carrying object-count callback plumbing inline. Existing `schema:minProperties` / `schema:maxProperties` tokens and `<subject> failed minProperties` / `maxProperties` errors are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputDependentRequiredFailures()` and `FirstJsonObjectDependentRequiredError()` so `dependentRequired` / legacy array-valued `dependencies` input/output wrappers share named adapter helpers instead of carrying dependency callback plumbing inline. Existing `field:dependentRequired:required` tokens and `<subject> field has invalid constraint: field:dependentRequired:required` errors are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputAdditionalPropertyFailures()` and `FirstJsonObjectAdditionalPropertyError()` so `additionalProperties:false` input/output wrappers share named adapter helpers instead of carrying extra-field callback plumbing inline. Existing `field:additionalProperties` tokens and `<subject> field has invalid constraint: field:additionalProperties` errors are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputNotRequiredFailures()` and `FirstJsonObjectNotRequiredError()` so object-level `not.required` input/output wrappers share named adapter helpers instead of carrying branch-match callback plumbing inline. Existing `schema:not:required` tokens and `<subject> failed not.required` errors are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputIfThenElseRequiredFailures()` and `FirstJsonObjectIfThenElseRequiredError()` so basic `if` / `then` / `else` required-field input/output wrappers share named adapter helpers instead of carrying conditional callback plumbing inline. Existing `schema:then:required:<field>` / `schema:else:required:<field>` tokens and `<subject> failed then required field: <field>` / `else required field` errors are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added named required-branch adapters for `allOf` and `anyOf` / `oneOf` checks: `CollectJsonObjectInputAllOfRequiredBranchFailures()`, `CollectJsonObjectInputRequiredBranchCountFailures()`, `FirstJsonObjectAllOfRequiredBranchError()`, and `FirstJsonObjectRequiredBranchCountError()`. Existing `schema:allOf:required` / `schema:anyOf:required` / `schema:oneOf:required` tokens and `<subject> failed <keyword> required branches` errors are preserved, including input's single-token `allOf` behavior. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputPropertyTypeFailures()` so skill-input `properties.*.type` validation uses a named collection adapter matching the existing JSON-object output `FirstJsonObjectPropertyTypeError()` helper. Existing `field:type` tokens and input/output type semantics are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `CollectJsonObjectInputRequiredFieldFailures()` and routed `JsonObjectInputRequiredFieldFailures()` through it, giving top-level required-field input validation the same named collection-adapter shape as the other schema helper families. Existing missing-field collection and `missing required input fields for <skill>` formatting are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added named string/numeric property-constraint adapters (`CollectJsonObjectInputStringConstraintFailures()`, `CollectJsonObjectInputNumericConstraintFailures()`, `FirstJsonObjectStringConstraintError()`, and `FirstJsonObjectNumericConstraintError()`) and routed the existing input/output wrappers through them. This preserves input's string-encoded argument semantics, output's native JSON value semantics, and the existing const/enum ordering difference while removing the remaining inline callback plumbing from those wrappers. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — added `FirstJsonObjectParsedValidationError()` and routed `JsonObjectSchemaValidationErrorForParsed()` plus `JsonObjectSchemaConstraintValidationErrorForParsed()` through it, so JSON-object output validation's required/type/constraint ordering uses one first-error combiner instead of repeated inline `if (!error.empty()) return error` chains. Public plugin output schema error order and text are preserved. Verified with MSVC focused schema/plugin tests and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended JSON-object output validation with native `properties.*.type=array` matching and array `minItems` / `maxItems` checks for plugin output schemas. Invalid array type and item-count failures now use the existing `PluginOutputSchemaValidationFailed` path with `invalid type: field:array` and `invalid constraint: field:minItems|maxItems` diagnostics. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended plugin JSON-object output schemas with array `items.type` validation. Array outputs whose elements do not match the declared item type now fail through the existing `PluginOutputSchemaValidationFailed` path with `invalid constraint: field:items:type`, while valid arrays continue through the normal structured `plugin_output` path. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended plugin JSON-object output schemas with string-valued array `items.const` and `items.enum` checks. Invalid item values now fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:items:const|enum`, while valid constant arrays still return structured `plugin_output`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended plugin JSON-object output schemas with string array `items.minLength`, `items.maxLength`, and `items.pattern` checks. Invalid item strings now fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:items:minLength|maxLength|pattern`, while valid patterned arrays still return structured `plugin_output`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended plugin JSON-object output schemas with numeric array item constraints (`items.minimum`, `items.maximum`, and `items.multipleOf`, with the shared evaluator also covering exclusive bounds). Invalid numeric items now fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:items:<constraint>`, while valid numeric arrays still return structured `plugin_output`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — added plugin JSON-object output `uniqueItems` validation for array properties. Duplicate JSON values now fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:uniqueItems`, while unique arrays still return structured `plugin_output`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — added plugin JSON-object output `contains`, `minContains`, and `maxContains` validation for array properties. Array elements are matched with the same supported scalar item schema subset used by `items`, no-match arrays fail with `invalid constraint: field:contains`, explicit lower bounds fail with `field:minContains`, and upper-bound excesses fail with `field:maxContains`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — added plugin JSON-object output `prefixItems` validation for array properties. Tuple-position item schemas now reuse the shared scalar item matcher, invalid indexed values fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:prefixItems`, and extra array entries remain unconstrained unless `items` is also declared. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended plugin JSON-object output array item validation to object-shaped `items` schemas. Object array entries now honor `items.required`, nested `items.properties.*.type` and scalar property constraints, `items.additionalProperties:false`, plus `items.minProperties` / `items.maxProperties`; invalid object entries fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:items:object`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — added `items.propertyNames` support for object-shaped plugin output array items. Invalid object item property names now fail through the same `PluginOutputSchemaValidationFailed` `invalid constraint: field:items:object` path used by other object item shape failures. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema companion cleanup slice — consolidated `propertyNames` keyword evaluation for top-level object validation and object-shaped array item matching through a shared `VisitJsonPropertyNameSchemaFailures()` visitor. Existing input/output property-name diagnostics and object-item `items:object` failures are preserved while removing the duplicate regex/length/const/enum evaluation path. Verified with MSVC focused schema/plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — extended object-shaped plugin output array item validation with `items.dependentRequired` and legacy array-valued `items.dependencies`. Object entries that omit dependency-required fields now fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:items:object`, while valid dependent object arrays still return structured `plugin_output`. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: ADR-JSON-001 schema keyword slice — added `items.not.required` support for object-shaped plugin output array items. Object entries that match a forbidden required-field branch now fail through `PluginOutputSchemaValidationFailed` with `invalid constraint: field:items:object`, while non-matching object entries still validate. Verified with MSVC focused plugin tests, `git diff --check`, and the full CTest suite.
- 2026-04-27: Added native `OpenAiAgent` REST adapter (`openai_agent.{hpp,cpp}`) targeting `https://api.openai.com/v1/chat/completions` with sync and V2 SSE streaming paths, modeled after `QwenAgent`. Default model `gpt-4o`. Registered in `main.cpp`, added to CMake build.
- 2026-04-27: Upgraded `OpenAiAuthProviderAdapter` to support `browser_oauth` mode with `refresh_token_supported=true`, enabling Codex OAuth PKCE login.
- 2026-04-27: Promoted `OAuthDefaultsForProvider(openai)` from `stub` to `builtin` with `auth.openai.com/authorize` and `auth.openai.com/oauth/token` endpoints plus `openid`/`offline_access` scopes.
- 2026-04-27: Updated `auth_tests.cpp` OAuth defaults test: OpenAI now asserted as `builtin`/`supported` alongside Gemini; only Anthropic and Qwen remain `stub`. 12/12 CTest targets pass.
- 2026-04-27: Phase O — added Interactive REPL mode (`agentos interactive`) and HTTP API server mode (`agentos serve`). REPL provides stdin command loop with run/agents/skills/status/memory/schedule subcommands, Ctrl-C interrupts. HTTP server exposes REST JSON API on configurable host:port (default `127.0.0.1:18080`) with `/api/health`, `/api/skills`, `/api/agents`, `/api/status`, `POST /api/run`, `/api/schedule/list`, `/api/memory/stats` endpoints. Default no-args behavior changed from demo to interactive. Added cpp-httplib v0.18.3 via FetchContent. Verified 87/87 build targets, 12/12 CTest tests, manual REPL session, and HTTP API curl validation.
- 2026-05-06: Added targeted `auto_decompose` plan-step routing. Planner outputs may now include optional `plan_steps[].role` or `plan_steps[].agent` fields; SubagentManager matches those steps to the corresponding role/agent before falling back to positional untargeted steps. The default `local_planner` now annotates plan steps with roles from decomposition context so the built-in offline planner exercises the same path. Verified with `cmake --build build-codex-g014 --target agentos_subagent_session_tests`, `ctest --test-dir build-codex-g014 -R agentos_subagent_session_tests --output-on-failure`, full `cmake --build build-codex-g014`, and full `ctest --test-dir build-codex-g014 --output-on-failure`.
- 2026-05-07: AutoDev retry counter slice — materialized tasks now persist `retry_count` and `max_retries`, repair-needed facts carry retry counters and `retry_limit_exceeded`, repair prompts show the current retry state, and exceeding the task retry limit blocks the job with a persisted blocker plus `autodev.repair.retry_limit_exceeded` event. CLI `autodev tasks`, `autodev repairs`, and `autodev summary` display retry state. Verified with `cmake --build build --target agentos_autodev_tests agentos_cli_integration_tests`, focused CTest for those targets, `git diff --check`, and the full 23-target CTest suite.
- 2026-05-07: AutoDev Codex app-server skeleton slice — added `CodexAppServerAutoDevAdapter` profile metadata for persistent-thread, native-event-stream, same-thread-repair capable execution while keeping `production_final_executor=false` and `healthy=false`. `autodev execute-next-task` now accepts `execution_adapter=codex_cli|codex_app_server`; both paths still record snapshot/blocked-turn facts and fail closed until real execution is implemented. Verified with `agentos_autodev_tests` and `agentos_cli_integration_tests`.
- 2026-05-07: AutoDev pause/resume/cancel slice — added `pause`, `resume`, and `cancel` job controls that only mutate AgentOS runtime state and append events (`autodev.job.paused`, `.resumed`, `.cancelled`); they explicitly do not interrupt, restart, or terminate a Codex process yet. CLI status and events expose the resulting state. Verified with focused AutoDev/CLI integration tests.
- 2026-05-07: AutoDev status watch slice — `autodev status --watch` and `autodev watch` now run a bounded polling view with `iterations` and `interval_ms`, showing status, phase, next action, event count, and latest event without relying on Codex self-reporting. Verified with CLI integration coverage and full CTest.
- 2026-05-07: AutoDev progress model slice — status and summary now display AgentOS-computed progress with phase weight, task passed/total, acceptance passed/total, and overall percent. The calculation uses runtime task facts and phase/job state rather than Codex self-reporting. Verified with CLI integration coverage.
- 2026-05-07: AutoDev multi-task acceptance edge-case slice — added regression coverage proving that accepting the first task in a multi-task job leaves the job in `codex_execution` with `execute_next_task`, while accepting the last task advances to `final_review` without marking the job done. Verified with `agentos_autodev_tests`.
- 2026-05-07: AutoDev final-review multi-task policy merge slice — extended multi-task regression coverage so final review fails when one task's `blocked_files` matches a changed file even if another task includes that file in `allowed_files`. This locks blocked-file precedence across merged task policy. Verified with `agentos_autodev_tests`.
- 2026-05-07: AutoDev machine-readable output slice — added `format=json` to `autodev summary`, `tasks`, `verifications`, `diffs`, `acceptances`, and `final-reviews`. JSON output reuses the runtime model serializers and includes job/progress/fact-count data for summary. Verified with CLI integration coverage for all six commands.
- 2026-05-07: AutoDev runtime state schema docs slice — added `docs/AUTODEV.md` documenting the current runtime store files, authoritative fact boundaries, query commands, and JSON output entry points.
- 2026-05-07: AutoDev AUTODEV_SPEC schema slice — added `docs/schemas/AUTODEV_SPEC.schema.json` and expanded `validate-spec` shape checks for `source_of_truth`, task object fields, string-array fields, `verify_command`, `max_retries`, and acceptance presence while preserving empty generated skeleton validation before approval. Verified with AutoDev tests.
- 2026-05-07: AutoDev docs/goal template hardening slice — generated goal docs now include `VERIFY.template.md`, `FINAL_REVIEW.template.md`, and `TASK.template.md` with explicit AgentOS Runtime Store authority boundaries. Verified with AutoDev goal-doc generation tests.
- 2026-05-07: AutoDev worktree cleanup policy slice — added `autodev cleanup-worktree job_id=<job_id>` for `worktree_cleanup_policy=keep_until_done`. Cleanup is gated to `done` / `cancelled` jobs, removes the job worktree, preserves AgentOS runtime facts, marks `isolation_status=cleaned`, and appends `autodev.worktree.cleaned`. Verified with CLI integration coverage.
- 2026-05-07: AutoDev blocked job recovery slice — added `autodev recover-blocked job_id=<job_id> [skill_pack_path=<path>]` to rerun recoverable setup gates from blocked `next_action` values. `status` and `summary` now show blocker/recovery guidance, summary works before `tasks.json` exists, and recovery remains conservative: no implicit spec approval and no Codex execution. CLI integration covers dirty workspace recovery, missing skill-pack recovery, spec-fix recovery, and explicit approval refusal.
- 2026-05-07: AutoDev real Codex CLI execution adapter slice — `autodev execute-next-task execution_adapter=codex_cli` now records the snapshot, builds a runtime prompt, invokes the configured Codex CLI command in the job worktree, and records a completed/failed turn with prompt artifact, response artifact, exit code, duration, and changed files. `codex_cli_command=<command>` and `AGENTOS_AUTODEV_CODEX_CLI_COMMAND` provide deterministic command override for tests and operators; task acceptance remains gated by verification, diff guard, and acceptance gate. Verified with CLI integration fixture coverage.
- 2026-05-07: AutoDev Codex app-server transport slice — `CodexAppServerAutoDevAdapter` now supports a configurable HTTP transport with `GET /health`, `POST /sessions`, and `POST /sessions/<session_id>/turns`. `execute-next-task execution_adapter=codex_app_server app_server_url=<url>` can open a persistent session, submit a turn, preserve returned event entries in the response artifact, and record the turn fact; without a URL it still fails closed. Verified with an in-process httplib app-server fixture and CLI integration regression coverage for the fail-closed path.
